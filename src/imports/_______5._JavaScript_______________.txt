Модуль 5. JavaScript: основы языка и работа с DOM

Теоретические материалы

Иконка Гиперссылка
JavaScript: основы языка и работа с DOM (презентация)
JavaScript: основы языка и работа с DOM
Иконка Страница
Введение в JavaScript: основы и синтаксис ES6 - ES14
Иконка Страница
Типы данных, функции, объекты
Иконка Страница
BOM (Browser Object Model)
Иконка Страница
Работа с элементами объектной модели документа DOM
Иконка Страница
Обработка событий
Иконка Страница
TypeScript- расширение возможностей JavaScript
Иконка Гиперссылка
Примеры кода Typescript
Иконка Страница
Транспайлер кода Babel
Лабораторные работы (для дневной формы обучения)
Лабораторная работа №5

Иконка Задание
Лабораторная работа №5 "Разработка интерактивного интерфейса на JavaScript"
Контроль знаний
Иконка Страница
Вопросы для самоподготовки
Вопросы для самоподготовки
Требуемые условия завершения
Тема 5: JavaScript: основы языка и работа с DOM
Что такое JavaScript и какова его основная роль в веб-разработке?

Какие способы подключения JavaScript к HTML-документу вы знаете?

Что такое переменная в JavaScript? Как объявить переменную (var, let, const)?

В чем разница между let, const и устаревшим var?

Какие примитивные типы данных существуют в JavaScript?

Что такое объект (Object) в JavaScript?

Как создать функцию в JavaScript? (Function Declaration, Function Expression, Arrow Function)

В чем отличие стрелочных функций от обычных?

Что такое область видимости (scope) в JavaScript?

Что такое замыкание (closure)? Приведите пример.

Что такое DOM (Document Object Model)?

Как выбрать элемент на странице с помощью JavaScript? (методы getElementById, querySelector и др.)

В чем разница между querySelector и querySelectorAll?

Как создать новый элемент и добавить его в DOM?

Как изменить текстовое содержимое элемента?

Как изменить HTML-содержимое элемента?

Как работать с атрибутами элемента (getAttribute, setAttribute)?

Как изменить CSS-стили элемента через JavaScript?

Как добавить класс к элементу и удалить класс?

Что такое событие (Event) в JavaScript?

Как добавить обработчик события к элементу?

Что такое объект события (Event Object) и какие свойства он содержит?

Что такое всплытие событий (Event Bubbling)?

Что такое делегирование событий (Event Delegation) и каковы его преимущества?

Как предотвратить поведение элемента по умолчанию (например, отправку формы)?

Как остановить всплытие события?

Что такое форма и как получить данные из полей формы с помощью JavaScript?

Как выполнить проверку (валидацию) данных формы на стороне клиента?

Что такое регулярные выражения (Regular Expressions) и как их использовать в JavaScript для проверки строк?

Как отладить JavaScript-код с помощью консоли браузера и точек останова (breakpoints)?
Лабораторная работа №5 "Разработка интерактивного интерфейса на JavaScript"
1. ЦЕЛЬ РАБОТЫ
Освоение основ языка JavaScript и практических навыков работы с DOM (Document Object Model). Формирование умения создавать интерактивные элементы веб-интерфейса, обрабатывать события и динамически изменять содержимое страницы.

2. НЕОБХОДИМЫЕ ЗНАНИЯ
Для успешного выполнения работы студент должен знать:

Основы языка программирования JavaScript:

Переменные, операторы, условные выражения и циклы.
Массивы и объекты.
Функции и обработка ошибок.
Работа с DOM:

Основные методы для выборки элементов на странице.
Изменение атрибутов, стилей и содержания элементов.
Добавление и удаление элементов.
Обработка событий:

Применение обработчиков событий (например, клики, отправка форм).
Использование таймеров, анимаций и задержек.
Валидация форм и работа с LocalStorage.

Основы HTML и CSS (из предыдущих лабораторных работ)

Основы работы с консолью разработчика в браузере
3. ЗАДАЧИ РАБОТЫ
Изучить основные операции работы с DOM (поиск элементов, манипуляция контентом и стилями)

Освоить обработку событий (click, input, submit и др.)

Реализовать интерактивные компоненты согласно индивидуальному варианту

Научиться работать с формами и валидацией данных

Организовать код в соответствии с лучшими практиками

Шаги выполнения лабораторной работы:

Введение в язык программирования JavaScript:

Студенты должны изучить основы JavaScript: синтаксис, работу с переменными, операторами, циклами, функциями и условными выражениями.
Применение JavaScript для манипуляции данными и выполнения различных операций.
Манипуляция элементами DOM:

Студенты должны изучить, что такое DOM и как он представляет структуру веб-страницы.
Овладение методами для выбора элементов (getElementById, querySelector и другие).
Изучение методов изменения содержания, стилей, атрибутов элементов и добавления/удаления элементов.
Обработка событий с помощью JavaScript:

Студенты должны научиться добавлять обработчики событий (например, нажатие кнопок, отправка формы).
Использование методов JavaScript для реагирования на пользовательские действия (click, change, submit).
Создание интерактивных элементов на веб-странице:

Применение полученных знаний для создания различных интерактивных элементов:
Формы с валидацией.
Слайдеры.
Выпадающие списки и другие элементы интерфейса.
Примерное задание:

Главная страница: Реализуйте форму добавления новости с сохранением в LocalStorage и обновлением списка новостей на странице. Реализуйте возможность удаления, редактирования новостей и поиска.
Страница с элементами: Реализуйте список элементов с возможностью добавления, редактирования и удаления. Используйте LocalStorage для хранения данных. Реализуйте сортировку по времени и названию.
Страница элемента: На странице элемента отобразите подробную информацию и реализуйте форму добавления отзывов с возможностью редактирования и удаления отзывов.
4. ПОРЯДОК ВЫПОЛНЕНИЯ РАБОТЫ
Часть 1. Подготовка проекта и настройка окружения

Перейдите в рабочую ветку:

 
git checkout main
git pull origin main
git checkout -b feature/javascript-interactivity
Создайте структуру файлов JavaScript:

В папке js создайте:

script.js - основной файл скриптов

components/ - папка для отдельных компонентов

utils/ - папка для вспомогательных функций

Подключите JavaScript к HTML:

html

Часть 2. Основы работы с DOM

Поиск элементов (в js/script.js):


Манипуляция контентом:


Работа с классами и стилями:

 

Часть 3. Обработка событий

Базовые события:

javascript

Делегирование событий:

 

Часть 4. Реализация индивидуального задания

Выберите и реализуйте задание согласно вашему варианту (см. ниже).

Часть 5. Работа с формами и валидация

Валидация формы:

 

Пример использования:

 

Часть 6. Рефакторинг и тестирование

Протестируйте функциональность в разных браузерах

Проверьте обработку ошибок

Убедитесь, что код соответствует стандартам ES6+

Часть 7. Фиксация изменений

 
git add js/
git add index.html
git commit -m "feat: add JavaScript interactivity for [тип проекта]"
git push origin feature/javascript-interactivity
5. СОДЕРЖАНИЕ ОТЧЕТА
Отчёт о выполнении лабораторной работы должен содержать:

Титульный лист

Цель работы

Краткие теоретические сведения о DOM, событиях и основах JavaScript

Ход работы:

Описание выполненных действий

Скриншоты:

Исходный код JavaScript в VS Code

Консоль браузера с выводами console.log

Работающее интерактивное поведение на странице

Инструменты разработчика, показывающие обработчики событий

Ссылка на коммит с выполненной работой

Ответы на контрольные вопросы

Выводы по работе

6. ТРЕБОВАНИЯ К ОФОРМЛЕНИЮ И СДАЧЕ
Выполненное задание и отчет должны быть представлены преподавателю для защиты

Электронная версия отчета (в формате PDF) должна быть загружена в систему СЭО

Отчет должен быть оформлен в соответствии со Стандартом Предприятия  (СТП)

Студенты работают в тех же бригадах, что и в предыдущих работах - Для выбора номера варианта для бригады используется Генератор вариантов.

1 подгруппа: выполняет варианты 1-8

2 подгруппа: выполняет варианты 9-16

ИНДИВИДУАЛЬНЫЕ ВАРИАНТЫ ЗАДАНИЙ
Задание для всех вариантов:
На основе созданной в предыдущих работах HTML-структуры и CSS-стилей реализуйте интерактивное поведение согласно вашему типу проекта. Используйте не менее 3 различных типов событий и реализуйте минимум 2 интерактивных компонента.

1 ПОДГРУППА (Варианты 1-8)

Вариант 1 - Новостной портал "NewsHub":

Реализуйте фильтрацию новостей по категориям

Добавьте "лайки" для новостей с сохранением состояния

Создайте модальное окно для подробного просмотра новости

Вариант 2 - Интернет-магазин "TechStore":

Реализуйте корзину покупок с добавлением/удалением товаров

Добавьте фильтры товаров по цене и характеристикам

Создайте слайдер для акционных товаров

Вариант 3 - Кулинарный блог "TastyBlog":

Реализуйте табы для переключения между рецептами

Добавьте таймер приготовления с уведомлениями

Создайте форму добавления комментариев

Вариант 4 - Сайт мероприятия "EventSpace":

Реализуйте календарь событий с выделением текущей даты

Добавьте форму регистрации на мероприятие

Создайте счетчик обратного отсчета до мероприятия

Вариант 5 - Социальная сеть "SocialSphere":

Реализуйте лайки и комментарии к постам

Добавьте бесконечную прокрутку ленты

Создайте модальное окно для создания нового поста

Вариант 6 - FAQ система "HelpDesk":

Реализуйте аккордеон для вопросов-ответов

Добавьте поиск по часто задаваемым вопросам

Создайте форму отправки нового вопроса

Вариант 7 - Портфолио фотографа "PhotoPortfolio":

Реализуйте модальное окно для просмотра фотографий

Добавьте фильтрацию фотографий по категориям

Создайте слайдер для портфолио

Вариант 8 - Образовательная платформа "EduPlatform":

Реализуйте прогресс-бар прохождения курса

Добавьте систему закладок для уроков

Создайте тест с проверкой ответов

2 ПОДГРУППА (Варианты 9-16)

Вариант 9 - Ресторан "GourmetResto":

Реализуйте интерактивное меню с фильтрацией по категориям

Добавьте форму бронирования столика с валидацией

Создайте слайдер для отзывов клиентов

Вариант 10 - Библиотека "DigitalLibrary":

Реализуйте поиск книг с автодополнением

Добавьте систему рейтинга и отзывов для книг

Создайте виртуальную "полку" для избранного

Вариант 11 - Киносайт "MovieHub":

Реализуйте рейтинговую систему для фильмов

Добавьте трейлеры во всплывающем окне

Создайте персонализированные рекомендации

Вариант 12 - Бронирование отелей "HotelBooking":

Реализуйте календарь выбора дат заезда/выезда

Добавьте фильтры отелей по удобствам и цене

Создайте калькулятор стоимости бронирования

Вариант 13 - Музыкальный сервис "MusicStream":

Реализуйте аудиоплеер с управлением воспроизведением

Добавьте создание и управление плейлистами

Создайте систему "лайков" для треков

Вариант 14 - SaaS платформа "CloudService":

Реализуйте переключение между тарифами с сравнением

Добавьте форму обратной связи с валидацией

Создайте анимированную статистику использования

Вариант 15 - Спортивный портал "SportArena":

Реализуйте таблицу результатов с сортировкой

Добавьте счетчик матчей в реальном времени

Создайте систему голосования за лучшего игрока

Вариант 16 - Научный журнал "ScienceJournal":

Реализуйте систему цитирования статей

Добавьте прогресс-бар чтения статьи

Создайте модальное окно для авторизации

КОНТРОЛЬНЫЕ ВОПРОСЫ
Для всех вариантов вопросы одинаковые:

Какие методы поиска элементов в DOM вы использовали в работе и в каких случаях каждый из них наиболее предпочтителен?
Как работает делегирование событий и какие преимущества оно дает при работе с динамически добавляемыми элементами?
Какие способы создания и добавления новых элементов в DOM вы применяли? В чем разница между innerHTML и createElement?
Как вы организовали структуру JavaScript-кода в проекте? Какие функции вы вынесли в отдельные модули и почему?
С какими особенностями обработки событий вы столкнулись и как решили проблемы всплытия и перехвата событий?
Что такое DOM и как он связан с веб-страницей?
Как можно выбрать элемент по его идентификатору в DOM?
Опишите, как можно изменить текст внутри элемента с помощью JavaScript.
Что такое обработчик событий в JavaScript и как его добавить к элементу?
Как получить доступ к значениям атрибутов элемента через JavaScript?
Какие существуют способы добавления нового элемента в DOM?
Как удалить элемент из DOM?
Что такое LocalStorage и как с ним работать в JavaScript?
Как можно сохранять и извлекать данные из LocalStorage?
Как можно обновить содержимое страницы без ее перезагрузки?
Какие методы JavaScript позволяют манипулировать стилями элементов на странице?
В чем заключается различие между методом getElementById и querySelector?
Как реализовать функциональность добавления нового элемента в список с помощью JavaScript?
Какие способы сортировки элементов на странице можно реализовать с помощью JavaScript?
Как реализовать простую форму валидации на JavaScript?
Как можно изменить класс элемента с помощью JavaScript?
Как обрабатывать событие клика на кнопке и выполнить определенное действие?
Что такое событие submit и как с ним работать в JavaScript?
Как получить значения, введенные пользователем в текстовое поле формы?
Как реализовать фильтрацию элементов на странице с помощью JavaScript?
Какие способы сортировки элементов по времени существуют в JavaScript?
Что такое анимация в контексте JavaScript и как она используется?
Как добавить кнопку "Наверх", которая плавно прокручивает страницу вверх?
Как реализовать слайдер изображений с помощью JavaScript?
Как создать выпадающий список с использованием JavaScript?
Как создать таймер, который отсчитывает определенное время в JavaScript?
Как создать калькулятор с возможностью выполнения различных операций на веб-странице?
Как реализовать функциональность перетаскивания элементов на веб-странице?
Какие методы используются для работы с изображениями через JavaScript (например, для их масштабирования)?
Как можно реализовать автоматическое обновление данных на веб-странице?
Как создать динамическую карту, отображающую местоположение на веб-странице?
ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ
Используйте console.log() для отладки кода

Обрабатывайте возможные ошибки с помощью try-catch

Используйте современный синтаксис ES6+ (стрелочные функции, деструктуризацию, шаблонные строки)

Следите за производительностью при работе с большим количеством элементов

Тестируйте код в разных браузерах

Примечание: Созданная в этой работе интерактивность будет расширена в следующих лабораторных работах при работе с асинхронными операциями и API.

Транспайлер кода Babel
Требуемые условия завершения
Babel
Babel — это транспайлер (преобразователь кода) для JavaScript, который позволяет использовать новейшие функции языка, даже если они ещё не поддерживаются браузерами или Node.js. Он преобразует современный код JavaScript в совместимый с более старыми версиями, что позволяет использовать последние стандарты языка, такие как ES6, ES7, и так далее.

Using Babel.js - Learning JavaScript Data Structures and Algorithms - Third  Edition [Book]

Основные особенности Babel:

Преобразует код из последней версии JavaScript в более старые, поддерживаемые версии.
Используется для компиляции ES6+ в ES5.
Поддерживает плагины для обработки различных синтаксических конструкций.
Поддерживает настройку через .babelrc файл или через конфигурацию в webpack.
Пример использования Babel:



Конфигурация Babel в .babelrc:

https://stackblitz.com/edit/solarlab-typescript-2021?file=index.ts
TypeScript- расширение возможностей JavaScript
Требуемые условия завершения
TypeScript - язык, расширяемый возможности JavaScript
The Major Benefits of Using Typescript | by Connor Finnegan | The Startup |  Medium

TypeScript — это надмножество JavaScript, которое добавляет статическую типизацию и другие возможности в стандартный JavaScript. Он был разработан компанией Microsoft с целью улучшить процесс разработки и масштабируемость приложений. TypeScript компилируется в обычный JavaScript, который исполняется в браузере или на сервере, благодаря чему он совместим с любым JavaScript-окружением.

TypeScript - это открытый исходный язык программирования, разработанный Microsoft, который является надмножеством JavaScript. TypeScript добавляет статическую типизацию и объектно-ориентированные возможности в JavaScript, что делает его более мощным и гибким для крупных проектов.

TypeScript — это мощный инструмент для разработчиков, который помогает создавать более стабильные и масштабируемые приложения. Он идеально подходит для крупных проектов, где важна строгая типизация и минимизация ошибок. TypeScript значительно улучшает опыт разработки, ускоряет создание качественного кода и помогает в поддержке приложений в долгосрочной перспективе.

Ключевые особенности TypeScript:
Инструменты разработчика: TypeScript предоставляет мощные инструменты разработчика, такие как автодополнение кода, подсказки при написании кода, поддержка рефакторинга и многое другое.
Поддержка сообщества: TypeScript активно поддерживается сообществом разработчиков и имеет широкий спектр инструментов и библиотек.
Статическая типизация:
TypeScript предоставляет статическую типизацию, т.е. позволяет задавать типы переменных, параметров функций и объектов. Это позволяет ловить ошибки на этапе компиляции, до того как код будет выполнен. Это особенно полезно для больших кодовых баз, где ошибки могут быть трудно обнаружить.


Типы могут быть как примитивными (number, string, boolean), так и более сложными (массива, объектов, кортежей и т. д.).
Совместимость с JavaScript:

TypeScript полностью совместим с JavaScript. Все существующие JavaScript-файлы можно использовать в TypeScript, и любой код, написанный на TypeScript, можно компилировать в чистый JavaScript.
TypeScript также поддерживает все новейшие возможности стандартов ECMAScript (ES6, ES7, и т. д.), такие как стрелочные функции, async/await, классы и модули.
Объектно-ориентированное программирование: TypeScript поддерживает концепции объектно-ориентированного программирования, такие как классы, интерфейсы и наследование и полиморфизм:

Пример интерфейса:


Декораторы:

TypeScript поддерживает декораторы, которые используются для добавления аннотаций или метаданных к классам, методам, свойствам и параметрам.
Декораторы часто используются в фреймворках, таких как Angular, для обработки и управления компонентами, свойствами и методами.
Типы данных:

TypeScript предлагает широкие возможности для работы с типами данных, включая объединенные типы (Union Types), перечисления (Enums), кортежи и типы типов (Generics).
Пример объединенных типов



Поддержка современных стандартов JavaScript:

TypeScript поддерживает все современные возможности JavaScript, такие как ES6+ (стрелочные функции, async/await, деструктуризация и другие).
Автоматическая компиляция и интеграция:

TypeScript компилируется в стандартный JavaScript, что означает, что его можно использовать на любом сервере или браузере, поддерживающем JavaScript.
TypeScript может быть интегрирован в любые сборщики, такие как Webpack и Gulp, что упрощает рабочие процессы в больших проектах.
Поддержка IDE и инструментов:

TypeScript тесно интегрирован с большинством современных IDE и редакторов кода, таких как Visual Studio Code, WebStorm, Sublime Text. Это позволяет использовать автодополнение, рефакторинг, проверку типов в реальном времени и другую функциональность, ускоряя процесс разработки.
Основные преимущества TypeScript:
Раннее обнаружение ошибок: TypeScript проверяет ошибки на этапе компиляции, что позволяет ловить их до того, как код попадет в продакшн.
Поддержка крупномасштабных приложений: TypeScript упрощает поддержку и развитие больших проектов за счет строгой типизации и четкой структуры кода.
Совместимость с JavaScript: Поскольку TypeScript является надмножеством JavaScript, он может быть использован в уже существующих JavaScript-проектах, постепенно внедряя статическую типизацию.
Лучшее авто-дополнение и подсказки: Поддержка типов позволяет улучшить функциональность автодополнения в IDE, что упрощает работу с кодом.
Меньше багов в коде: Типы помогают избежать распространенных ошибок, таких как несоответствия типов или отсутствие ожидаемых свойств у объектов.
Состав TypeScript
TypeScript состоит из трех основных компонентов:

Язык: TypeScript включает в себя синтаксис, ключевые слова и аннотации типов.
Компилятор TypeScript: Компилятор TypeScript (tsc) преобразует инструкции, написанные на TypeScript, в их эквивалент на JavaScript.
Сервис языка TypeScript: Сервис языка TypeScript предоставляет дополнительный слой вокруг основного компилятора, который поддерживает общие операции редактора, такие как автодополнение, подсказки, форматирование кода и многое другое.
Пример использования TypeScript:



TypeScript vs JavaScript
Типизация: TypeScript добавляет статическую типизацию, чего нет в чистом JavaScript. Это помогает ловить ошибки на ранних стадиях разработки.
Компиляция: TypeScript должен быть скомпилирован в JavaScript перед выполнением, в отличие от JavaScript, который интерпретируется непосредственно браузером.
Кодирование: TypeScript предоставляет расширенные возможности для ООП, декораторов и других паттернов проектирования, которые могут быть полезны для разработки сложных приложений.
Применение TypeScript:
Web-приложения: TypeScript активно используется в крупных веб-приложениях и фреймворках, таких как Angular, React и Vue.js.
Серверная разработка: TypeScript может использоваться в серверных приложениях, например, с Node.js.
Инструменты и библиотеки: TypeScript используется в популярных инструментах и библиотеках, таких как NestJS, Deno, и в других проектах, требующих надежной типизации.
Обработка событий
Требуемые условия завершения
События в JavaScript
События — это действия, которые происходят в браузере и могут быть использованы для выполнения кода. Это может быть клик по кнопке, наведение на элемент, изменение значения в поле и т. д.

Браузерные события и примеры их использования в JavaScript · ИТ Шеф

Основные типы событий:
click — срабатывает при клике на элемент.
keydown / keyup — срабатывает при нажатии/отпускании клавиши.
mouseover / mouseout — срабатывают при наведении мыши на элемент и уходе с него.
submit — срабатывает при отправке формы.
change — срабатывает при изменении значения в поле ввода.
focus / blur — срабатывают при фокусе и потере фокуса на элементе.
Обработка событий в JavaScript
1. Основы обработки событий
Типы событий и их классификация
 

Способы добавления обработчиков событий
Добавление обработчиков событий
Существует несколько способов добавления обработчиков событий:

inline event handlers (не рекомендуется):


 
HTML-атрибуты (устаревший способ)

 
Через HTML DOM (старый стиль):

 
DOM-свойства (традиционный способ)
 

 
Через addEventListener (рекомендуется):

addEventListener (современный способ)
 

 
Метод addEventListener() позволяет добавлять несколько обработчиков событий на один элемент и предоставляет дополнительные возможности, такие как управление фазами события (всплытие и захват).

Объект события (Event Object)
 

Всплытие событий (Event Bubbling)
Всплытие событий — это механизм, при котором событие начинает срабатывать с самого вложенного элемента и "всплывает" вверх по дереву DOM, пока не достигнет корня документа (например, document).

Пример:


2. Всплытие и перехват событий
Фазы распространения событий
 

 
Управление распространением событий
 

Практические примеры управления событиями
 

Когда пользователь кликает по кнопке, сработает сначала обработчик для кнопки (Child clicked), а затем — для родительского элемента (Parent clicked), поскольку событие всплывает.

Делегирование событий
Делегирование событий — это подход, при котором обработчик события прикрепляется к родительскому элементу, а не к каждому дочернему. Это особенно полезно, если необходимо обрабатывать динамически добавленные элементы.

Пример:


 
В этом примере обработчик события добавлен к родительскому элементу ul. Когда пользователь кликает на один из элементов списка, событие делегируется родителю, и обрабатывается с учетом того, что именно был кликнут элемент с классом item.

3. Делегирование событий
Основы делегирования событий
 

Утилиты для делегирования событий
 

Практические примеры делегирования
 

Захват событий (Event Capturing)
Захват — это фаза, в которой событие сначала передается от корня DOM дерева до целевого элемента, прежде чем событие начнет всплывать обратно. Чтобы использовать захват, при добавлении обработчика необходимо указать третий параметр true.

 
Модификация поведения события
Используя объект события, можно отменить его стандартное поведение или остановить дальнейшее распространение.

event.preventDefault() — отменяет стандартное поведение события (например, отправку формы).
event.stopPropagation() — останавливает дальнейшее распространение события по дереву DOM (не позволяет событию всплывать).
Пример:


События и асинхронность
События могут быть асинхронными, что позволяет выполнять код без блокировки потока исполнения. Это важная концепция для создания интерактивных приложений.


 
События в асинхронных функциях
Многие события могут быть связаны с асинхронными операциями, например, при работе с API или загрузке данных.


 
4. Пользовательские события
Создание и диспетчеризация пользовательских событий
 

Практическое использование пользовательских событий
 



5. События жизненного цикла
События загрузки страницы
 

События производительности
 

6. Передовые техники обработки событий
Throttling и Debouncing
 

Паттерн Event Bus
 



Обработка ошибок в событиях
 
Работа с элементами объектной модели документа DOM
Требуемые условия завершения
Работа с DOM (Document Object Model) и событиями — это важные аспекты веб-разработки, так как взаимодействие с DOM позволяет создавать динамичные, интерактивные веб-страницы.

Что такое DOM?
DOM (Document Object Model) — это объектная модель документа, которая представляет структуру HTML или XML документа в виде дерева объектов. Каждый элемент, атрибут и текст внутри HTML-документа представлен как объект. С помощью JavaScript можно изменять эти объекты, добавлять новые элементы, удалять старые и т. д.

HTML DOM (Document Object Model) - GeeksforGeeks

События в DHTML
События в DHTML (Dynamic HTML) позволяют JavaScript добавлять обработчики событий к HTML-элементам. Например, вы можете добавить обработчик события click к кнопке, чтобы выполнить некоторую функцию, когда пользователь нажимает на кнопку.

Структура языка ядро (ECMAScript, объектная модель документа DOM)


JavaScript основан на стандарте ECMAScript, который определяет основные возможности языка. DOM (Document Object Model) - это интерфейс, который позволяет JavaScript взаимодействовать с HTML-документом. DOM представляет собой дерево объектов, каждый из которых представляет HTML-элемент на странице.

Иерархия объектов браузера
В JavaScript существуют различные объекты, которые представляют различные части браузера. Например, объект window представляет окно браузера, а объект document представляет HTML-документ. Эти объекты и их свойства, методы и события могут быть использованы для взаимодействия с браузером и HTML-документом.

Свойства, методы и события объекта window
Объект window представляет окно браузера. Он имеет множество свойств, методов и событий, которые можно использовать для взаимодействия с браузером. Например, свойство location представляет текущий URL, метод alert() показывает всплывающее окно, а событие load срабатывает, когда страница полностью загружена.

Свойства, методы и события объекта document
Объект document представляет HTML-документ. Он также имеет множество свойств, методов и событий, которые можно использовать для взаимодействия с HTML-документом. Например, свойство body представляет тело HTML-документа, метод getElementById() позволяет найти элемент по его ID, а событие DOMContentLoaded срабатывает, когда HTML-документ полностью загружен.

Использование объектов Math, Date, String
JavaScript предоставляет множество встроенных объектов, которые могут быть использованы для выполнения различных задач. Например, объект Math предоставляет функции для выполнения математических операций, объект Date позволяет работать с датами и временем, а объект String предоставляет функции для работы со строками.

Основные методы работы с DOM
document.getElementById(id) — возвращает элемент с заданным id.
document.getElementsByClassName(className) — возвращает коллекцию всех элементов с заданным классом.
document.getElementsByTagName(tagName) — возвращает коллекцию всех элементов с заданным тегом.
document.querySelector(selector) — возвращает первый элемент, который соответствует CSS-селектору.
document.querySelectorAll(selector) — возвращает все элементы, которые соответствуют CSS-селектору.
1. Выборка элементов
Основные методы выборки
getElementById
 

getElementsByClassName
 

getElementsByTagName
 

querySelector
 

querySelectorAll
 

Манипуляции с DOM
innerHTML — позволяет изменять или получать HTML-содержимое элемента.
textContent — позволяет изменять или получать текстовое содержимое элемента.
setAttribute() — позволяет изменить атрибут элемента.
appendChild() — добавляет новый дочерний элемент.
removeChild() — удаляет дочерний элемент.
Пример:


 
Специализированные методы выборки
Выборка форм и элементов форм
 

Выборка из Shadow DOM
 

Современные методы и производительность
 

2. Манипуляция контентом
Работа с текстовым содержимым
textContent vs innerText
 

innerHTML
 

Создание и добавление элементов
createElement и append
 

insertAdjacentHTML и аналогичные методы
 

Удаление и клонирование элементов
 

Работа с атрибутами
 

3. Манипуляция стилями
Работа с классами CSS
classList методы
 

Прямое управление стилями
style свойство
 

Получение вычисленных стилей
 

CSS-переменные (Custom Properties)
 

Анимации и переходы
 



Современные подходы и лучшие практики
 


BOM (Browser Object Model)
Требуемые условия завершения
BOM (Browser Object Model) — это объектная модель, которая представляет взаимодействие между браузером и JavaScript. В отличие от DOM (Document Object Model), который работает с содержимым веб-страницы (HTML), BOM управляет взаимодействием с браузером и его окружением. Это набор объектов, с помощью которых можно управлять состоянием окна браузера, его размером, а также взаимодействовать с другими возможностями браузера, такими как история, URL, и другие компоненты.

Browser Object Model

Объектная модель браузера (Browser Object Model, BOM) — это набор объектов, которые предоставляют доступ к различным компонентам браузера и позволяют взаимодействовать с ними через JavaScript. BOM отличается от DOM (Document Object Model), который работает с содержимым веб-страницы, так как BOM предоставляет доступ к объектам, не связанным напрямую с контентом страницы.

Основные компоненты BOM:


window

window является главным объектом BOM. Это глобальный объект JavaScript, который представляет окно браузера. Все остальные объекты, связанные с браузером, такие как document, location, history и другие, являются свойствами этого объекта.
Пример: window.alert('Привет!'); — вызов окна с сообщением.
document

Хотя объект document больше связан с DOM, его можно рассматривать и как часть BOM, поскольку это интерфейс для взаимодействия с содержимым страницы.
location

Объект location предоставляет информацию о текущем URL страницы, а также позволяет изменять его, переходить на другие страницы или перезагружать текущую страницу.
Пример: location.href = 'https://example.com'; — перенаправление на другой сайт.
history

Объект history позволяет взаимодействовать с историей браузера. Это позволяет, например, переходить между страницами в рамках одной сессии, а также изменять или просматривать историю посещённых страниц.
Пример: history.back(); — переход на предыдущую страницу.
navigator

Объект navigator предоставляет информацию о браузере и его возможностях, таких как поддерживаемые плагины, язык, и параметры устройства.
Пример: navigator.userAgent — строка, содержащая информацию о браузере.
screen

Объект screen предоставляет информацию о разрешении экрана пользователя и его характеристиках.
Пример: screen.width — ширина экрана пользователя.
setTimeout() и setInterval()

setTimeout() и setInterval() — это методы BOM, которые позволяют устанавливать таймеры. setTimeout() выполняет функцию один раз через указанный интервал времени, а setInterval() — повторяет выполнение функции через регулярные интервалы.
alert(), confirm(), prompt()

Эти функции являются частью BOM и предоставляют взаимодействие с пользователем через модальные окна.
alert() — отображает простое всплывающее окно с сообщением.
confirm() — отображает диалог с кнопками "ОК" и "Отмена", возвращает true или false.
prompt() — отображает окно с полем для ввода текста, возвращает строку, введённую пользователем.
Работа с BOM позволяет веб-разработчикам создавать более динамичные и интерактивные веб-приложения, а также управлять поведением браузера без необходимости напрямую изменять содержимое страницы.

Как BOM используется в веб-разработке?
Управление окнами браузера: Вы можете управлять размером окна, его расположением, а также открывать и закрывать окна с помощью JavaScript.
Работа с историей и навигацией: BOM позволяет создавать динамичные веб-приложения с возможностью манипуляции с историей браузера без полной перезагрузки страницы.
Интерактивность и обработка времени: Вы можете создавать асинхронные таймеры, запланированные действия и даже анимации, которые выполняются через определённые интервалы времени.
Адаптация контента под устройство: BOM может помочь вам определить разрешение экрана пользователя или его предпочтения (например, язык браузера), чтобы сделать сайт более адаптивным и персонализированным.

Типы данных, функции, объекты
Требуемые условия завершения
Типы данных, функции, объекты
1. Типы данных в JavaScript
JavaScript поддерживает несколько типов данных, в том числе:

Примитивные типы:
JavaScript имеет несколько примитивных типов данных, которые важно знать:

Примитивы: string, number, boolean, undefined, null, symbol (в ES6), bigint (в ES2020).
Ссылочные типы:
object (массивы, функции, даты, и другие структуры данных.)
Пример:



Примитивные типы данных
Number (числа)

Числа в JavaScript могут быть целыми и с плавающей точкой (десятичными). Для представления чисел не требуются кавычки.

Пример:






String (строки)
Строки — это последовательности символов. В JavaScript строки можно задавать с использованием:

Одинарных кавычек: 'Hello'
Двойных кавычек: "Hello"
Обратных кавычек (для шаблонных строк): `Hello`
Пример:


 


Шаблонные строки (Template Literals)
Многострочные строки и интерполяция
 
// Старый способ
const oldWay = "Hello " + name + "!\n" +
               "Your score is: " + score + "\n" +
               "Thank you for playing!";

// Новый способ с шаблонными строками
const newWay = `Hello ${name}!
Your score is: ${score}
Thank you for playing!`;

// Выражения внутри шаблонов
const a = 5, b = 10;
console.log(`The sum is: ${a + b}`); // "The sum is: 15"

// Вызовы функций
function formatCurrency(amount) {
    return `$${amount.toFixed(2)}`;
}

const price = 19.99;
console.log(`Total: ${formatCurrency(price)}`); // "Total: $19.99"

// Вложенные шаблоны
const isMember = true;
const discount = 0.1;
console.log(`Price: $${isMember ? price * (1 - discount) : price}`); 
 
Boolean (логический тип)
 
// Прямые значения
let isTrue = true;
let isFalse = false;

// Преобразование к boolean
console.log(Boolean(1)); // true
console.log(Boolean(0)); // false
console.log(Boolean("")); // false
console.log(Boolean("hello")); // true
console.log(Boolean(null)); // false
console.log(Boolean(undefined)); // false
console.log(Boolean([])); // true (особенность JavaScript)

// Логические операторы
let a = true, b = false;
console.log(a && b); // false (И)
console.log(a || b); // true (ИЛИ)
console.log(!a); // false (НЕ)

// Короткий цикл вычислений
console.log(true && "Hello"); // "Hello"
console.log(false && "Hello"); // false
console.log(true || "Hello"); // true
console.log(false || "Hello"); // "Hello"

// Практическое использование
function greet(name) {
    name = name || "Guest";
    return `Hello, ${name}!`;
}
console.log(greet("John")); // "Hello, John!"
console.log(greet()); // "Hello, Guest!"
Undefined и Null
 
// Undefined - переменная объявлена, но не инициализирована
let undefinedVar;
console.log(undefinedVar); // undefined
console.log(typeof undefinedVar); // "undefined"

// Null - явное отсутствие значения
let nullVar = null;
console.log(nullVar); // null
console.log(typeof nullVar); // "object" (историческая особенность)

// Различия
console.log(undefined == null); // true (нестрогое сравнение)
console.log(undefined === null); // false (строгое сравнение)

// Практическое использование
function findUser(id) {
    // Если пользователь не найден, возвращаем null
    if (!userExists(id)) {
        return null;
    }
    return getUserData(id);
}

let user = findUser(123);
if (user === null) {
    console.log("User not found");
}
Symbol (уникальные идентификаторы)
 
// Создание символов
let sym1 = Symbol();
let sym2 = Symbol("description");
let sym3 = Symbol("description");

console.log(sym1 === sym2); // false
console.log(sym2 === sym3); // false (даже с одинаковым описанием)

// Использование в объектах
let user = {
    name: "John",
    [Symbol("id")]: 123 // скрытое свойство
};

console.log(Object.keys(user)); // ["name"] - символ не виден

// Глобальный реестр символов
let globalSym = Symbol.for("global");
let sameGlobalSym = Symbol.for("global");
console.log(globalSym === sameGlobalSym); // true

// Well-known symbols
let array = [1, 2, 3];
let iterator = array[Symbol.iterator]();
console.log(iterator.next()); // {value: 1, done: false}
BigInt (большие числа)
 
// Создание BigInt
let big1 = 123456789012345678901234567890n;
let big2 = BigInt("123456789012345678901234567890");

// Операции
console.log(big1 + big2);
console.log(big1 * big2);

// Нельзя смешивать с обычными числами
let regularNumber = 100;
// console.log(big1 + regularNumber); // Ошибка
console.log(big1 + BigInt(regularNumber)); // Правильно

// Использование
const MAX_SAFE_INTEGER = Number.MAX_SAFE_INTEGER;
console.log(MAX_SAFE_INTEGER); // 9007199254740991
console.log(MAX_SAFE_INTEGER + 1 === MAX_SAFE_INTEGER + 2); // true!

const bigMax = BigInt(MAX_SAFE_INTEGER);
console.log(bigMax + 1n === bigMax + 2n); // false
Ссылочные типы данных
Object (объекты)
 
// Объект - коллекция ключ-значение
let person = {
    name: "Alice",
    age: 30,
    isStudent: false,
    address: {
        city: "New York",
        zipCode: "10001"
    },
    hobbies: ["reading", "swimming"],
    
    // Методы
    greet: function() {
        return `Hello, I'm ${this.name}`;
    },
    
    // Сокращенный синтаксис методов (ES6)
    celebrateBirthday() {
        this.age++;
        return `Now I'm ${this.age} years old!`;
    }
};

// Доступ к свойствам
console.log(person.name); // "Alice" (точечная нотация)
console.log(person["age"]); // 30 (скобочная нотация)

// Динамические ключи
let key = "name";
console.log(person[key]); // "Alice"

// Добавление и изменение свойств
person.email = "alice@example.com";
person["phone"] = "+1234567890";

// Удаление свойств
delete person.isStudent;

// Проверка существования свойства
console.log("name" in person); // true
console.log(person.hasOwnProperty("name")); // true
Массивы и методы массивов
Массивы — это упорядоченные коллекции значений, которые могут быть разных типов (строки, числа, объекты и даже другие массивы). Массивы в JavaScript индексируются с нуля.

Они могут содержать любой тип данных, включая другие массивы.



Пример:


JavaScript предоставляет множество встроенных методов для работы с массивами:

map() — создает новый массив с результатами вызова функции на каждом элементе массива.
filter() — создает новый массив с элементами, которые прошли тест в функции.
reduce() — сводит все элементы массива к одному значению.
forEach() — выполняет функцию для каждого элемента массива.
find() — находит первый элемент массива, который удовлетворяет условию.
Пример:


 
// Создание массивов
let fruits = ["apple", "banana", "orange"];
let numbers = new Array(1, 2, 3);
let mixed = [1, "hello", true, { name: "John" }];

// Доступ к элементам
console.log(fruits[0]); // "apple"
console.log(fruits[fruits.length - 1]); // "orange" (последний элемент)

// Основные методы массивов
fruits.push("grape"); // Добавить в конец
console.log(fruits); // ["apple", "banana", "orange", "grape"]

let lastFruit = fruits.pop(); // Удалить последний
console.log(lastFruit); // "grape"

fruits.unshift("strawberry"); // Добавить в начало
console.log(fruits); // ["strawberry", "apple", "banana", "orange"]

let firstFruit = fruits.shift(); // Удалить первый
console.log(firstFruit); // "strawberry"

// Поиск в массиве
console.log(fruits.indexOf("banana")); // 2
console.log(fruits.includes("apple")); // true

// Преобразование массива
let upperCaseFruits = fruits.map(fruit => fruit.toUpperCase());
console.log(upperCaseFruits); // ["APPLE", "BANANA", "ORANGE"]

let longFruits = fruits.filter(fruit => fruit.length > 5);
console.log(longFruits); // ["banana", "orange"]

let totalLength = fruits.reduce((total, fruit) => total + fruit.length, 0);
console.log(totalLength); // 16

// Сортировка
let sortedFruits = fruits.sort();
console.log(sortedFruits); // ["apple", "banana", "orange"]

let numbersArray = [3, 1, 4, 1, 5, 9];
numbersArray.sort((a, b) => a - b);
console.log(numbersArray); // [1, 1, 3, 4, 5, 9]

Set и Map
Set и Map — это новые структуры данных в ES6.

Set — это коллекция уникальных значений.
Map — это коллекция пар ключ-значение, где ключи могут быть любого типа.
Пример:


 
 
2. Функции
Области видимости (Scope)
Глобальная область видимости
 
let globalVar = "I'm global";

function accessGlobal() {
    console.log(globalVar); // "I'm global"
    globalVar = "Modified globally";
}

accessGlobal();
console.log(globalVar); // "Modified globally"
Локальная область видимости (function scope)
 
function demonstrateScope() {
    let localVar = "I'm local";
    var oldStyleVar = "I'm function scoped";
    
    if (true) {
        let blockScoped = "I'm block scoped";
        var stillFunctionScoped = "I'm still accessible";
        console.log(localVar); // "I'm local"
    }
    
    console.log(stillFunctionScoped); // "I'm still accessible"
    // console.log(blockScoped); // Ошибка ReferenceError
}

demonstrateScope();
// console.log(localVar); // Ошибка ReferenceError
Блочная область видимости (block scope)
 
// let и const имеют блочную область видимости
function blockScopeExample() {
    if (true) {
        let blockVar = "I exist only in this block";
        const BLOCK_CONST = "Me too";
        console.log(blockVar); // Работает
    }
    // console.log(blockVar); // Ошибка ReferenceError
    // console.log(BLOCK_CONST); // Ошибка ReferenceError
}

// Циклы с блочной областью видимости
for (let i = 0; i < 3; i++) {
    setTimeout(() => {
        console.log(i); // 0, 1, 2
    }, 100);
}

// Сравнение с var
for (var j = 0; j < 3; j++) {
    setTimeout(() => {
        console.log(j); // 3, 3, 3
    }, 100);
}
Функции в JavaScript
Функции — это основной способ упаковки логики и повторного использования кода. Функции - это блоки кода, которые можно вызывать по имени. Они могут принимать аргументы и возвращать значения. Например:




Обычные функции

 
// Различные способы создания функций

// Function Declaration
function sayHello(name) {
    return `Hello, ${name}!`;
}

// Function Expression
const sayGoodbye = function(name) {
    return `Goodbye, ${name}!`;
};

// Arrow Function
const add = (a, b) => a + b;

// Параметры и аргументы
function greet(greeting = "Hello", name = "Guest") {
    return `${greeting}, ${name}!`;
}

console.log(greet()); // "Hello, Guest!"
console.log(greet("Hi", "John")); // "Hi, John!"

// Arguments object
function sum() {
    let total = 0;
    for (let i = 0; i < arguments.length; i++) {
        total += arguments[i];
    }
    return total;
}
console.log(sum(1, 2, 3, 4)); // 10

// Rest parameters (современная альтернатива arguments)
function modernSum(...numbers) {
    return numbers.reduce((total, num) => total + num, 0);
}
console.log(modernSum(1, 2, 3, 4)); // 10

Стрелочные функции (Arrow Functions)
В ES6 появилась новая запись для функций — стрелочные функции.

Стрелочные функции предоставляют более компактную запись и не имеют собственного this.


 
Сравнение с обычными функциями
 
// Обычная функция
function traditionalFunction(a, b) {
    return a + b;
}

// Стрелочная функция
const arrowFunction = (a, b) => {
    return a + b;
};

// Сокращенный синтаксис
const shortArrow = (a, b) => a + b;

// Без параметров
const noParams = () => console.log("Hello!");

// Один параметр
const oneParam = x => x * 2;
Контекст this в стрелочных функциях
 
class Counter {
    constructor() {
        this.count = 0;
        
        // Проблема с обычной функцией
        setInterval(function() {
            // this здесь - не экземпляр Counter!
            // this.count++; // Ошибка!
        }, 1000);
        
        // Решение со стрелочной функцией
        setInterval(() => {
            this.count++; // this берется из внешней области!
            console.log(this.count);
        }, 1000);
    }
}

// Еще пример с обработчиками событий
const button = document.querySelector('button');

// Обычная функция - свой this
button.addEventListener('click', function() {
    console.log(this); // Сам элемент button
});

// Стрелочная функция - this из внешней области
button.addEventListener('click', () => {
    console.log(this); // window или undefined (в strict mode)
});
 
Функции высшего порядка (Higher-order functions)
Функции высшего порядка — это функции, которые принимают другие функции в качестве аргументов или возвращают функции.

Пример:


 

 
// Функции, которые принимают или возвращают другие функции

// Функция, принимающая функцию как аргумент
function operateOnArray(arr, operation) {
    const result = [];
    for (let i = 0; i < arr.length; i++) {
        result.push(operation(arr[i]));
    }
    return result;
}

const numbers = [1, 2, 3, 4, 5];
const doubled = operateOnArray(numbers, x => x * 2);
console.log(doubled); // [2, 4, 6, 8, 10]

const squared = operateOnArray(numbers, x => x * x);
console.log(squared); // [1, 4, 9, 16, 25]

// Функция, возвращающая функцию
function createMultiplier(multiplier) {
    return function(x) {
        return x * multiplier;
    };
}

const double = createMultiplier(2);
const triple = createMultiplier(3);

console.log(double(5)); // 10
console.log(triple(5)); // 15

// Каррирование (currying)
function curry(fn) {
    return function curried(...args) {
        if (args.length >= fn.length) {
            return fn.apply(this, args);
        } else {
            return function(...args2) {
                return curried.apply(this, args.concat(args2));
            };
        }
    };
}

function addThree(a, b, c) {
    return a + b + c;
}

const curriedAdd = curry(addThree);
console.log(curriedAdd(1)(2)(3)); // 6
console.log(curriedAdd(1, 2)(3)); // 6
console.log(curriedAdd(1, 2, 3)); // 6
 
Date (даты)
 
// Создание дат
let now = new Date();
let specificDate = new Date("2024-01-15");
let timestamp = new Date(1700000000000);

// Форматирование дат
console.log(now.toDateString()); // "Mon Jan 15 2024"
console.log(now.toISOString()); // "2024-01-15T10:30:00.000Z"
console.log(now.toLocaleDateString()); // "15.01.2024" (зависит от локали)

// Получение компонентов даты
console.log(now.getFullYear()); // 2024
console.log(now.getMonth()); // 0 (январь, месяцы 0-11)
console.log(now.getDate()); // 15
console.log(now.getDay()); // 1 (понедельник, 0-6)
console.log(now.getHours()); // 13
console.log(now.getMinutes()); // 30

// Установка компонентов даты
let futureDate = new Date();
futureDate.setFullYear(2025);
futureDate.setMonth(11); // декабрь

// Разница между датами
let start = new Date("2024-01-01");
let end = new Date("2024-01-15");
let difference = end - start; // в миллисекундах
console.log(difference / (1000 * 60 * 60 * 24)); // 14 дней
Определение типов данных
 
// Оператор typeof
console.log(typeof 42); // "number"
console.log(typeof "hello"); // "string"
console.log(typeof true); // "boolean"
console.log(typeof undefined); // "undefined"
console.log(typeof null); // "object" (особенность)
console.log(typeof {}); // "object"
console.log(typeof []); // "object"
console.log(typeof function() {}); // "function"

// Более точная проверка типов
function getType(value) {
    if (value === null) return "null";
    if (Array.isArray(value)) return "array";
    return typeof value;
}

console.log(getType(null)); // "null"
console.log(getType([])); // "array"
console.log(getType({})); // "object"

// Проверка на число (включая NaN)
function isNumber(value) {
    return typeof value === "number" && !isNaN(value);
}

console.log(isNumber(42)); // true
console.log(isNumber(NaN)); // false
console.log(isNumber("42")); // false
 
Замыкания (Closures)
Замыкание — это механизм, при котором функция имеет доступ к своим внешним переменным, даже после того как внешний контекст завершит выполнение.

Замыкание - это функция, которая имеет доступ к переменным из своего внешнего (родительского) скоупа, даже после того, как внешний скоуп завершил свое выполнение. Это позволяет сохранять состояние между вызовами функций




Пример:



 
// Базовое замыкание
function createCounter() {
    let count = 0;
    
    return function() {
        count++;
        return count;
    };
}

const counter = createCounter();
console.log(counter()); // 1
console.log(counter()); // 2
console.log(counter()); // 3

// Практическое использование - приватные переменные
function createBankAccount(initialBalance) {
    let balance = initialBalance;
    
    return {
        deposit: function(amount) {
            balance += amount;
            return balance;
        },
        withdraw: function(amount) {
            if (amount <= balance) {
                balance -= amount;
                return balance;
            }
            return "Insufficient funds";
        },
        getBalance: function() {
            return balance;
        }
    };
}

const account = createBankAccount(1000);
console.log(account.getBalance()); // 1000
console.log(account.deposit(500)); // 1500
console.log(account.withdraw(200)); // 1300
// console.log(account.balance); // undefined (приватная переменная)
Рекурсивные функции
 
// Факториал
function factorial(n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

console.log(factorial(5)); // 120

// Числа Фибоначчи
function fibonacci(n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(6)); // 8

// Глубокое копирование объекта
function deepCopy(obj) {
    if (obj === null || typeof obj !== "object") {
        return obj;
    }
    
    if (Array.isArray(obj)) {
        return obj.map(item => deepCopy(item));
    }
    
    const copied = {};
    for (let key in obj) {
        if (obj.hasOwnProperty(key)) {
            copied[key] = deepCopy(obj[key]);
        }
    }
    
    return copied;
}

const original = { a: 1, b: { c: 2, d: [3, 4] } };
const copied = deepCopy(original);
console.log(copied); // { a: 1, b: { c: 2, d: [3, 4] } }
console.log(original.b === copied.b); // false (разные объекты)
 
3. Объекты
Объекты — это коллекции свойств, где каждое свойство состоит из ключа (строка) и значения (любого типа).
Создание объектов
Пример:


 
Объект this
Ключевое слово this в JavaScript ссылается на объект, к которому принадлежит выполняемая функция. Оно часто вызывает путаницу, особенно в контексте разных типов функций, например, стрелочных функций (которые не имеют своего this, а наследуют его от внешней области).

Пример:


Современные литералы объектов
Сокращенный синтаксис
 
const name = "John";
const age = 30;

// Старый способ
const oldUser = {
    name: name,
    age: age,
    speak: function() {
        console.log("Hello!");
    }
};

// Новый способ
const newUser = {
    name,        // автоматически: name: name
    age,         // автоматически: age: age
    speak() {    // сокращенный метод
        console.log("Hello!");
    },
    // Вычисляемые свойства
    [`${name}Score`]: 100
};

console.log(newUser); // { name: "John", age: 30, JohnScore: 100 }
или 
 
const person = {
    firstName: "John",
    lastName: "Doe",
    age: 30,
    
    getFullName() {
        return `${this.firstName} ${this.lastName}`;
    },
    
    celebrateBirthday() {
        this.age++;
        console.log(`Happy birthday! Now you are ${this.age}`);
    }
};
 
Конструктор объектов
 
function Person(firstName, lastName, age) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
}

Person.prototype.getFullName = function() {
    return `${this.firstName} ${this.lastName}`;
};

Person.prototype.celebrateBirthday = function() {
    this.age++;
    console.log(`Happy birthday! Now you are ${this.age}`);
};

const john = new Person("John", "Doe", 30);
console.log(john.getFullName()); // "John Doe"
Классы в JavaScript
Классы были введены в ES6 и позволяют использовать объектно-ориентированное программирование (ООП) в JavaScript.

// Старый способ с функциями-конструкторами
function OldPerson(name, age) {
    this.name = name;
    this.age = age;
}

OldPerson.prototype.speak = function() {
    console.log(`Hello, I'm ${this.name}`);
};

Создание класса

 
Конструктор (constructor) — это специальный метод, который вызывается при создании экземпляра класса.
Методы класса добавляются непосредственно в его тело.
 
// Новый способ с классами
class Person {
    constructor(name, age) {
        this.name = name;
        this.age = age;
    }
    
    // Методы добавляются в прототип
    speak() {
        console.log(`Hello, I'm ${this.name}`);
    }
    
    // Геттеры и сеттеры
    get birthYear() {
        return new Date().getFullYear() - this.age;
    }
    
    set birthYear(year) {
        this.age = new Date().getFullYear() - year;
    }
    
    // Статические методы
    static compareAges(person1, person2) {
        return person1.age - person2.age;
    }
}

// Использование
const john = new Person("John", 30);
john.speak(); // "Hello, I'm John"
console.log(john.birthYear); // 1994 (примерно)

const alice = new Person("Alice", 25);
console.log(Person.compareAges(john, alice)); // 5
 
 
class Person {
    constructor(firstName, lastName, age) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.age = age;
    }
    
    getFullName() {
        return `${this.firstName} ${this.lastName}`;
    }
    
    celebrateBirthday() {
        this.age++;
        console.log(`Happy birthday! Now you are ${this.age}`);
    }
    
    // Статический метод
    static compareAges(person1, person2) {
        return person1.age - person2.age;
    }
    
    // Геттер
    get fullName() {
        return this.getFullName();
    }
    
    // Сеттер
    set fullName(name) {
        const [firstName, lastName] = name.split(" ");
        this.firstName = firstName;
        this.lastName = lastName;
    }
}

const alice = new Person("Alice", "Smith", 25);
console.log(alice.fullName); // "Alice Smith"
alice.fullName = "Alice Johnson";
console.log(alice.lastName); // "Johnson"
Прототипное наследование и классы (ES6+)
До ES6 в JavaScript использовалось прототипное наследование, при котором объекты наследуют свойства и методы от других объектов. Однако в ES6 были введены классы, которые предоставляют более структурированный и понятный синтаксис для создания и наследования объектов. Классы в ES6 — это синтаксический сахар, который работает поверх прототипного наследования.

Пример использования классов:



Наследование классов
JavaScript поддерживает наследование классов, что позволяет создавать новые классы на основе существующих.


 
class Animal {
    constructor(name) {
        this.name = name;
    }
    
    speak() {
        console.log(`${this.name} makes a sound`);
    }
}

class Dog extends Animal {
    constructor(name, breed) {
        super(name); // вызов конструктора родителя
        this.breed = breed;
    }
    
    // Переопределение метода
    speak() {
        console.log(`${this.name} barks`);
    }
    
    // Новый метод
    fetch() {
        console.log(`${this.name} fetches the ball`);
    }
}

const dog = new Dog("Rex", "Labrador");
dog.speak(); // "Rex barks"
dog.fetch(); // "Rex fetches the ball"
 
// Прототипное наследование
function Animal(name) {
    this.name = name;
}

Animal.prototype.speak = function() {
    console.log(`${this.name} makes a sound`);
};

function Dog(name, breed) {
    Animal.call(this, name); // Вызов конструктора родителя
    this.breed = breed;
}

// Наследование прототипа
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

Dog.prototype.bark = function() {
    console.log(`${this.name} barks!`);
};

// Переопределение метода
Dog.prototype.speak = function() {
    console.log(`${this.name} the ${this.breed} says: Woof!`);
};

const rex = new Dog("Rex", "Labrador");
rex.speak(); // "Rex the Labrador says: Woof!"
rex.bark(); // "Rex barks!"

// Проверка цепочки прототипов
console.log(rex instanceof Dog); // true
console.log(rex instanceof Animal); // true
console.log(rex instanceof Object); // true
Современные методы массивов и объектов
 
const user = {
    name: "John",
    age: 30,
    email: "john@example.com"
};

// Object.keys(), Object.values(), Object.entries()
console.log(Object.keys(user)); // ["name", "age", "email"]
console.log(Object.values(user)); // ["John", 30, "john@example.com"]
console.log(Object.entries(user)); // [["name", "John"], ["age", 30], ["email", "john@example.com"]]

// Object.assign() - копирование свойств
const userWithPhone = Object.assign({}, user, { phone: "123-456-7890" });
console.log(userWithPhone);

// Object.freeze() - заморозка объекта
const frozenUser = Object.freeze(user);
// frozenUser.age = 31; // Ошибка в strict mode
console.log(frozenUser.age); // 30

// Object.seal() - запрет добавления/удаления свойств
const sealedUser = Object.seal(user);
sealedUser.age = 31; // Можно изменять
// sealedUser.phone = "123"; // Ошибка
console.log(sealedUser.age); // 31

// Object.defineProperty() - определение свойств с дескрипторами
const config = {};
Object.defineProperty(config, "apiKey", {
    value: "secret123",
    writable: false,
    enumerable: false,
    configurable: false
});

console.log(config.apiKey); // "secret123"
// config.apiKey = "newkey"; // Ошибка
console.log(Object.keys(config)); // [] (не перечисляемое)

Новые методы массивов
 
const numbers = [1, 2, 3, 4, 5];

// Array.prototype.find() и findIndex()
const firstEven = numbers.find(num => num % 2 === 0);
console.log(firstEven); // 2

const firstEvenIndex = numbers.findIndex(num => num % 2 === 0);
console.log(firstEvenIndex); // 1

// Array.prototype.includes()
console.log(numbers.includes(3)); // true
console.log(numbers.includes(6)); // false

// Array.prototype.flat() и flatMap()
const nested = [1, [2, 3], [4, [5, 6]]];
console.log(nested.flat()); // [1, 2, 3, 4, [5, 6]]
console.log(nested.flat(2)); // [1, 2, 3, 4, 5, 6]

const phrases = ["hello world", "good morning"];
const words = phrases.flatMap(phrase => phrase.split(" "));
console.log(words); // ["hello", "world", "good", "morning"]
Object.assign() и Object.entries()
 
const target = { a: 1, b: 2 };
const source = { b: 3, c: 4 };

// Копирование свойств
const result = Object.assign(target, source);
console.log(result); // { a: 1, b: 3, c: 4 }

// Object.entries() для итерации
const person = { name: "John", age: 30 };
for (const [key, value] of Object.entries(person)) {
    console.log(`${key}: ${value}`);
}

// Object.values()
const values = Object.values(person);
console.log(values); // ["John", 30]

// Object.keys()
const keys = Object.keys(person);
console.log(keys); // ["name", "age"]
Деструктуризация объектов и массивов
Деструктуризация позволяет извлекать значения из объектов и массивов в отдельные переменные с помощью специального синтаксиса.

Пример для массива:


Деструктуризация массивов
 
// Базовый синтаксис
const numbers = [1, 2, 3, 4, 5];
const [first, second] = numbers;
console.log(first, second); // 1 2

// Пропуск элементов
const [,, third] = numbers;
console.log(third); // 3

// Значения по умолчанию
const [a = 10, b = 20] = [1];
console.log(a, b); // 1 20

// Остаточные параметры
const [head, ...tail] = numbers;
console.log(head); // 1
console.log(tail); // [2, 3, 4, 5]

// Обмен переменных
let x = 1, y = 2;
[x, y] = [y, x];
console.log(x, y); // 2 1
Деструктуризация объектов
Пример для объекта:



 
const user = {
    id: 1,
    name: "John Doe",
    email: "john@example.com",
    address: {
        city: "New York",
        zip: "10001"
    }
};

// Базовый синтаксис
const { name, email } = user;
console.log(name, email); // "John Doe" "john@example.com"

// Переименование переменных
const { name: userName, email: userEmail } = user;
console.log(userName, userEmail);

// Значения по умолчанию
const { phone = "No phone" } = user;
console.log(phone); // "No phone"

// Вложенная деструктуризация
const { address: { city, zip } } = user;
console.log(city, zip); // "New York" "10001"

// В функциях
function printUser({ name, email }) {
    console.log(`${name} (${email})`);
}
printUser(user); // "John Doe (john@example.com)"


или 
const user = {
    id: 1,
    personalInfo: {
        name: "Alice",
        age: 25
    },
    contacts: {
        email: "alice@example.com",
        phone: "+1234567890"
    },
    preferences: ["dark theme", "notifications"]
};

// Базовая деструктуризация
const { name, age } = user.personalInfo;
console.log(name, age); // "Alice" 25

// Деструктуризация с переименованием
const { name: userName, age: userAge } = user.personalInfo;
console.log(userName, userAge); // "Alice" 25

// Значения по умолчанию
const { theme = "light" } = user.preferences || {};
console.log(theme); // "light"

// Вложенная деструктуризация
const { 
    personalInfo: { name: firstName }, 
    contacts: { email } 
} = user;
console.log(firstName, email); // "Alice" "alice@example.com"

// Деструктуризация в параметрах функции
function sendEmail({ personalInfo: { name }, contacts: { email } }, message) {
    console.log(`Sending to ${name} (${email}): ${message}`);
}

sendEmail(user, "Hello!"); // "Sending to Alice (alice@example.com): Hello!"

// Деструктуризация с rest оператором
const { id, ...userWithoutId } = user;
console.log(userWithoutId); // { personalInfo: {...}, contacts: {...}, preferences: [...] }
Параметры по умолчанию и остаточные параметры
Параметры по умолчанию
 
// Старый способ
function oldGreet(name) {
    name = name || "Guest";
    return "Hello " + name;
}

// Новый способ
function greet(name = "Guest", greeting = "Hello") {
    return `${greeting}, ${name}!`;
}

console.log(greet()); // "Hello, Guest!"
console.log(greet("John")); // "Hello, John!"
console.log(greet("Alice", "Hi")); // "Hi, Alice!"

// Выражения как значения по умолчанию
function createUser(name, id = generateId(), createdAt = new Date()) {
    return { name, id, createdAt };
}

function generateId() {
    return Math.random().toString(36).substr(2, 9);
}
Остаточные параметры (Rest Parameters)
 
// Старый способ с arguments
function oldSum() {
    let total = 0;
    for (let i = 0; i < arguments.length; i++) {
        total += arguments[i];
    }
    return total;
}

// Новый способ с rest parameters
function sum(...numbers) {
    return numbers.reduce((total, num) => total + num, 0);
}

console.log(sum(1, 2, 3)); // 6
console.log(sum(1, 2, 3, 4, 5)); // 15

// Комбинация с обычными параметрами
function calculate(operation, ...numbers) {
    switch(operation) {
        case 'add':
            return numbers.reduce((a, b) => a + b, 0);
        case 'multiply':
            return numbers.reduce((a, b) => a * b, 1);
        default:
            return 0;
    }
}

console.log(calculate('add', 1, 2, 3)); // 6
console.log(calculate('multiply', 2, 3, 4)); // 24
 
JSON и объекты
 
const person = {
    name: "John",
    age: 30,
    hobbies: ["reading", "swimming"],
    isStudent: false,
    birthDate: new Date(),
    address: {
        city: "New York",
        zipCode: "10001"
    }
};

// Преобразование в JSON
const jsonString = JSON.stringify(person);
console.log(jsonString);
// {"name":"John","age":30,"hobbies":["reading","swimming"],"isStudent":false,"birthDate":"2024-01-15T10:00:00.000Z","address":{"city":"New York","zipCode":"10001"}}

// Кастомное преобразование
const customJson = JSON.stringify(person, (key, value) => {
    if (key === "age") return undefined; // Исключаем возраст
    if (value instanceof Date) return value.toLocaleDateString(); // Форматируем дату
    return value;
}, 2);

console.log(customJson);

// Парсинг JSON
const parsedPerson = JSON.parse(jsonString);
console.log(parsedPerson.name); // "John"
console.log(parsedPerson.birthDate); // строка, а не Date объект

// Парсинг с ревивером
const revivedPerson = JSON.parse(jsonString, (key, value) => {
    if (key === "birthDate") return new Date(value);
    return value;
});

console.log(revivedPerson.birthDate instanceof Date); // true

Введение в JavaScript: основы и синтаксис ES6 - ES14
Требуемые условия завершения
JavaScript — это язык программирования, который используется для создания интерактивных и динамичных веб-страниц. Он является основным языком для клиентской стороны, но также активно используется на серверной стороне (например, с помощью Node.js). В последние годы JavaScript активно развивался, и новые возможности, введенные с ES6 (или ECMAScript 2015) и последующих версий, значительно улучшили синтаксис и функциональные возможности языка.

JavaScript — Википедия

Область применения
JavaScript широко используется в веб-разработке для создания интерактивных элементов на веб-страницах, обработки событий, работы с DOM (Document Object Model). Он также может быть использован на серверной стороне через Node.js, что позволяет создавать бэкенд-приложения и работать с базами данных, а также обрабатывать HTTP-запросы.

https://survey.stackoverflow.co/2025/technology/ :



ECMAScript 6 (ES6)
ECMAScript 6, также известный как ES6 или ES2015, является важной версией стандарта ECMAScript, на основе которого разработан язык JavaScript.

ECMAScript 6 / ES6 New Features - Tutorial 1 - Let - YouTube

ECMAScript 2015 (ES6) принес революционные изменения в JavaScript, добавив новые возможности синтаксиса и функциональность, которые делают код более читаемым, поддерживаемым и эффективным:

Классы (classes),
Модули (modules),
Генераторы (generators),
Промисы (promises),
Новые возможности для работы с функциями, такие как стрелочные функции (arrow functions),
Шаблонные строки (template literals) и многое другое.
Эти нововведения значительно улучшили язык, повысив его гибкость, читаемость и удобство использования.

Современные возможности ES7-ES10
ES2016 (ES7)
Руководство по ES2016 (ES7) | Frontend Stuff - нажми на изображение

 

ES2017 (ES8)
Руководство по ES2017 (ES8) | Frontend Stuff - нажми на изображение

 

ES2018 (ES9)
Руководство по ES2018 (ES9) | Frontend Stuff - нажми на изображение

 

ES2019 (ES10)
JavaScript: What's new in ECMAScript 2019 (ES2019)/ES10? | by Selvaganesh |  Medium - нажми на изображение

 

Современный JavaScript (ES2020-ES2023)
ES2020 (ES11)
Что нового в JavaScript, новый стандарт ES2020/ES11 функции с примерами  кода - YouTube - нажми на изображение

 

ES2021 (ES12)
JavaScript ES2021 / ES12 новые возможности | Frontend Stuff - нажми на изображение

 

ES2022 (ES13)
JavaScript ES2022 / ES13 новые возможности | Frontend Stuff - нажми на изображение

 

ES2023 (ES14)
JavaScript ES2023 / ES14 новые возможности | Frontend Stuff  - нажми на изображение

 

Практические примеры современного JavaScript
Модульная система
 

Работа с асинхронностью
 

Функциональное программирование
 

Современный JavaScript предлагает мощные инструменты для написания чистого, читаемого и эффективного кода. Ключевые преимущества:

Более выразительный синтаксис (стрелочные функции, деструктуризация, шаблонные строки)

Улучшенная работа с асинхронностью (async/await, Promise)

Лучшая организация кода (модули, классы)

Более безопасные конструкции (const/let, приватные поля)

Удобные методы работы с данными (новые методы массивов и объектов)

Связь с другими языками
JavaScript имеет схожий синтаксис с языками, такими как C, Java, и Python, что делает его доступным для разработчиков, знакомых с этими языками. Однако JavaScript также поддерживает функциональное и объектно-ориентированное программирование, что делает его гибким и многозадачным языком. Его тесная интеграция с HTML и CSS также делает его незаменимым инструментом для разработки веб-приложений.

 
Поддержка браузерами
JavaScript поддерживается всеми современными веб-браузерами, включая Google Chrome, Mozilla Firefox, Safari, Microsoft Edge, а также (хотя и с некоторыми ограничениями) Internet Explorer. Большинство современных веб-сайтов используют JavaScript для создания интерактивных элементов, таких как формы, динамическая загрузка контента и анимации, а также для улучшения пользовательского опыта.

Внедрение JavaScript в HTML-документ
JavaScript может быть встроен непосредственно в HTML-документ с помощью тега <script>. 
Встроенный скрипт (inline script)



Также JavaScript может быть помещен в отдельный файл и подключен к HTML-документу с помощью тега <script> и атрибута src.
Подключение внешнего файла




Инлайн через атрибуты (onclick, onload)

Модули ES6 через import/export

Семантика и синтаксис
JavaScript использует C-подобный синтаксис, что означает использование фигурных скобок {} для блоков кода и точку с запятой ; для завершения инструкций (хотя в большинстве случаев JavaScript автоматически добавляет точку с запятой). Язык обладает динамической типизацией, что означает, что типы переменных определяются в процессе выполнения, а не во время компиляции.

Константы и переменные
До ES6 в JavaScript для объявления переменных использовались var и let. Однако в ES6 появились более строгие способы объявления переменных с использованием let и const:

var — функциональная область видимости, устаревший способ объявления переменных, обладающий проблемами с областью видимости (неблоковая область видимости).
let — переменная с блочной областью видимости, которая может быть изменена после инициализации.
const — переменная с блочной областью видимости, значение которой нельзя изменить после инициализации (константа), но свойства объектов, объявленных через const, могут изменяться)
let/const не подвержены hoisting'у как var

const требует инициализации и не может быть перезаписан

var можно переобъявлять, let/const — нет

Устаревший подход с var
 

Современный подход с let и const
 

Область видимости переменных
Область видимости определяет доступность переменных. Область видимости переменной определяет, в какой части программы она доступна.



В JavaScript существует несколько типов области видимости:

Бывает: глобальная, функциональная (для var), блочная (для let/const).

Глобальная область видимости — переменные доступны во всей программе.
Блочная область видимости (при использовании let и const) — переменные доступны только в пределах блока, где они были объявлены.
Функциональная область видимости — переменные доступны только внутри функций, в которых они были объявлены.
Выражения
Выражение - это комбинация одного или более операторов и операндов. Например, 2 * 3 + 4 - это выражение, которое складывает результат умножения двух чисел.

Операторы
Операторы - это специальные символы, которые выполняют определенные математические или логические операции. Например, + - это оператор сложения, - - оператор вычитания, * - оператор умножения, / - оператор деления, % - оператор остатка от деления .

Арифметические: +, -, *, /, %
Сравнения: ==, ===, !=, !==, >, <, >=, <=
Логические: &&, ||, !
Присваивания: =, +=, -=, *=, /=
Операторы распространения (Spread / Rest)
Spread используется для извлечения элементов из массива или объекта, а Rest — для сбора оставшихся значений.

Пример с массивом (Spread):

Работа с массивами
 


 
Пример с объектом (Spread):

Работа с объектами
 


Пример с функцией (Rest):



Комментарии
Комментарии в JavaScript начинаются с // для однострочных комментариев и /* ... */ для многострочных комментариев.

Однострочные комментарии: // Это комментарий
Многострочные комментарии: /* Это многострочный комментарий */
Модули в JavaScript 
(ES6 Modules)
В ES6 был введен стандарт для работы с модулями в JavaScript. Модули позволяют организовывать код в отдельные файлы и использовать функциональность через import/export.

Экспорт
 

Импорт
 

Пример:



Экспорт и импорт

 
Модули позволяют организовать код в более структурированный и поддерживаемый вид, что особенно важно для крупных приложений.
Условия
Условные операторы позволяют выполнять разные блоки кода в зависимости от выполнения условий:

if, else if, else
switch — используется для сравнения значений нескольких вариантов.
Пример:



Тернарный оператор
Тернарный оператор — это сокращенная форма для условных операторов:


Циклы
Циклы позволяют повторять выполнение блока кода несколько раз:

for — для стандартных циклов с заранее известным числом итераций.
while — выполняется, пока условие истинно.
do...while — выполняется хотя бы один раз, затем проверяется условие.
Пример:


 
Современные циклы и итераторы
for...of для массивов и итерируемых объектов
 


for...in для объектов (с осторожностью!)
 

 
 
 
Типы ошибок
JavaScript предоставляет разные типы ошибок:

SyntaxError: Ошибка синтаксиса.
ReferenceError: Ошибка, когда пытаемся обратиться к несуществующей переменной.
TypeError: Ошибка неправильного типа данных.

https://frontend-stuff.com/blog/es2023/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
JavaScript ES2023 / ES14 новые возможности
Что нового в ECMAScript 2023. Расширение возможностей JavaScript.

27.05.2024
·
2 мин чтения
В 2023 году JavaScript получил ряд обновлений, вошедших в стандарт ES2023. Ниже — самые заметные из них.

Array.prototype.findLast
Array.prototype.findLastIndex
Array.prototype.toReversed
Array.prototype.toSorted
Array.prototype.toSpliced
Array.prototype.with
Итог
Array.prototype.findLast
Метод findLast находит последний элемент в массиве, который удовлетворяет заданному условию. Это удобный способ поиска элементов с конца массива, что иногда необходимо в сложных структурах данных или при работе с большими массивами.

// findLast(callbackFn, thisArg)

const array = [1, 2, 3, 4, 5];
const lastEven = array.findLast(element => element % 2 === 0);

console.log(lastEven); // 4
Copy
В этом примере findLast ищет последний четный элемент в массиве. Это особенно полезно, когда порядок элементов важен и требуется обработка данных с конца массива.

Array.prototype.findLastIndex
Метод findLastIndex возвращает индекс последнего элемента в массиве, который удовлетворяет заданному условию. Он похож на findLast, но возвращает индекс, а не сам элемент.

// findLastIndex(callbackFn, thisArg)

const array = [1, 2, 3, 4, 5];
const lastEvenIndex = array.findLastIndex(element => element % 2 === 0);

console.log(lastEvenIndex); // 3
Copy
Этот метод полезен, когда нужно не только найти последний элемент, соответствующий условию, но и знать его позицию в массиве для дальнейшей обработки или манипуляций.

Array.prototype.toReversed
Метод toReversed возвращает новый массив с элементами в обратном порядке, не изменяя исходный массив. Это удобный способ получить перевернутую версию массива без необходимости изменять исходный массив.

const numbers = [1, 2, 3];
const reversed = numbers.toReversed();

console.log(reversed); // [3, 2, 1]
console.log(numbers);  // [1, 2, 3]
Copy
В этом примере toReversed создает новый массив с элементами в обратном порядке, оставляя исходный массив неизменным. Это особенно полезно для операций, требующих перевернутого порядка данных.

Array.prototype.toSorted
Метод toSorted возвращает новый отсортированный массив, не изменяя исходный. Это упрощает сортировку массивов без риска изменить исходные данные.

// toSorted(compareFn)

const numbers = [3, 1, 2];
const sorted = numbers.toSorted();

console.log(sorted);  // [1, 2, 3]
console.log(numbers); // [3, 1, 2]
Copy
Этот метод полезен, когда нужно получить отсортированную версию массива, но исходные данные должны оставаться неизменными для дальнейшего использования.

Array.prototype.toSpliced
Метод toSpliced позволяет вырезать и вставлять элементы в массив, возвращая новый массив без изменения исходного. Это расширяет возможности манипуляций с массивами без риска изменения исходных данных.

// toSpliced(start, deleteCount, item1, item2, /* …, */ itemN)

const numbers = [1, 2, 3, 4];
const spliced = numbers.toSpliced(1, 2, 5, 6);

console.log(spliced); // [1, 5, 6, 4]
console.log(numbers); // [1, 2, 3, 4]
Copy
Здесь toSpliced создает новый массив, в котором элементы с 1 по 2 индекс заменены на 5 и 6. Исходный массив остается неизменным, что позволяет безопасно использовать исходные данные в других частях программы.

Array.prototype.with
Метод with позволяет создать новый массив, изменив один элемент по заданному индексу, не изменяя исходный массив. Это полезно для работы с неизменяемыми данными, где изменение данных должно быть выполнено аккуратно и безопасно.

// arrayInstance.with(index, value)

const numbers = [1, 2, 3];
const newArray = numbers.with(1, 4);

console.log(newArray); // [1, 4, 3]
console.log(numbers);  // [1, 2, 3]
Copy
В этом примере метод with создает новый массив, где элемент с индексом 1 заменен на 4. Исходный массив остается неизменным, что делает этот метод идеальным для ситуаций, требующих работы с неизменяемыми данными.

Итог
ES2023 добавил шесть новых методов массивов: findLast, findLastIndex, toReversed, toSorted, toSpliced и with. Общая логика у них одна — работать с массивами без мутации исходных данных.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
23.05.2024
JavaScript ES2022 / ES13 новые возможности
3 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
17.02.2022
JavaScript ES2021 / ES12 новые возможности
6 мин чтения
← Старше
JavaScript ES2022 / ES13 новые возможности
23.05.2024
Новее →
Как обновить свою ветку последними изменениями с основной ветки
03.06.2024
Copyright © 2026. Frontend Stuff


https://frontend-stuff.com/blog/es2022/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
JavaScript ES2022 / ES13 новые возможности
Что нового в ECMAScript 2022. Расширение возможностей JavaScript.

23.05.2024
·
3 мин чтения
В 2022 году вышла спецификация ES2022 (ECMAScript 2022), также известная как ES13. Ниже — основные изменения и новые возможности.

Методы Array.prototype.at()
Методы Object.hasOwn()
Логические операторы присваивания
Топ-уровневое await
Методы Error.cause
Методы RegExp Match Indices
Promise.any
Итог
Методы Array.prototype.at()
Метод at() предоставляет удобный способ доступа к элементам массива с использованием положительных и отрицательных индексов. Ранее, чтобы получить последний элемент массива, мы использовали конструкцию arr[arr.length - 1]. Теперь это можно сделать проще.

const array = ['JavaScript', 'Python', 'Ruby', 'Go'];

console.log(array.at(0)); // Выводит: "JavaScript"
console.log(array.at(-1)); // Выводит: "Go"
Copy
Этот метод упрощает доступ к последним элементам, особенно когда точный размер массива неизвестен, делая код более читаемым и элегантным.

Методы Object.hasOwn()
Метод Object.hasOwn() упрощает проверку наличия собственных свойств объекта — более прямая альтернатива Object.prototype.hasOwnProperty.call(), без конфликтов с собственными свойствами объекта.

const obj = { a: 1, b: 2 };

console.log(Object.hasOwn(obj, 'a')); // true
console.log(Object.hasOwn(obj, 'c')); // false
Copy
Логические операторы присваивания
ES2022 добавил три новых логических оператора присваивания: &&=, ||= и ??=. Они упрощают логические операции и делают код более читаемым.

&&= выполняет присваивание, если значение истинно.
||= выполняет присваивание, если значение ложно.
??= выполняет присваивание, если значение равно null или undefined.
let a = true;
let b = false;
let c = null;

a &&= false; // a = false
b ||= true;  // b = true
c ??= 3;     // c = 3
Copy
Топ-уровневое await
С ES2022 стало возможным использование await на верхнем уровне модулей, упрощая структуру кода и избавляя от необходимости оборачивать асинхронные вызовы в анонимные самовызывающиеся функции. Это значительно улучшает читаемость и поддержку кода, делая асинхронную логику более интуитивно понятной.

// example.mjs
const data = await fetch('https://api.example.com/data');
const jsonData = await data.json();

console.log(jsonData);
Copy
Методы Error.cause
Методы Error.cause позволяют передавать дополнительную информацию о причине ошибки. Это улучшает отладку и делает сообщения об ошибках более информативными.

try {
  throw new Error('Something went wrong', { cause: 'Invalid input' });
} catch (err) {
  console.error(err.message); // "Something went wrong"
  console.error(err.cause);   // "Invalid input"
}
Copy
Методы RegExp Match Indices
Добавлены методы для работы с индексами сопоставления в регулярных выражениях. Метод matchAll возвращает итератор с подробной информацией о каждом совпадении, включая индексы. Чтобы получить индексы совпадений, нужно использовать флаг d в регулярном выражении.

const regex = /t(e)(st(\d?))/gd;
const str = 'test1test2';

const matches = [...str.matchAll(regex)];

for (const match of matches) {
  console.log(`Full match: ${match[0]} at [${match.indices[0][0]}, ${match.indices[0][1]}]`);
  console.log(`First capturing group: ${match[1]} at [${match.indices[1][0]}, ${match.indices[1][1]}]`);
  console.log(`Second capturing group: ${match[2]} at [${match.indices[2][0]}, ${match.indices[2][1]}]`);
  console.log(`Third capturing group: ${match[3]} at [${match.indices[3][0]}, ${match.indices[3][1]}]`);
}

// Full match: test1 at [0, 5]
// First capturing group: e at [1, 2]
// Second capturing group: st1 at [2, 5]
// Third capturing group: 1 at [4, 5]

// Full match: test2 at [5, 10]
// First capturing group: e at [6, 7]
// Second capturing group: st2 at [7, 10]
// Third capturing group: 2 at [9, 10]
Copy
Как это работает:

Флаг d: Новый флаг d в регулярном выражении используется для отслеживания индексов совпадений.
Метод matchAll: Возвращает итератор, который содержит объект с информацией о каждом совпадении, включая индексы.
Promise.any
Метод Promise.any() ожидает исполнения одного из переданных промисов и возвращает его результат. Если ни один из промисов не выполнится успешно, возвращается ошибка AggregateError.

const promises = [
  Promise.reject('Ошибка'),
  Promise.resolve('Первый успешный ответ'),
  new Promise((resolve) => setTimeout(resolve, 100, 'Второй успешный ответ'))
];

Promise.any(promises).then((value) => console.log(value));
// Выводит: "Первый успешный ответ"
Copy
Итог
ES2022 добавил несколько точечных, но полезных вещей: удобный доступ к элементам по индексу через at(), прямую проверку свойств через Object.hasOwn(), top-level await, причины ошибок через Error.cause, индексы совпадений в регулярных выражениях и Promise.any. Каждое нововведение закрывает конкретную неудобность языка.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
17.02.2022
JavaScript ES2021 / ES12 новые возможности
6 мин чтения
← Старше
JAMstack: современный подход к веб-разработке
26.04.2023
Новее →
JavaScript ES2023 / ES14 новые возможности
27.05.2024
Copyright © 2026. Frontend Stuff


https://frontend-stuff.com/blog/es2022/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
JavaScript ES2022 / ES13 новые возможности
Что нового в ECMAScript 2022. Расширение возможностей JavaScript.

23.05.2024
·
3 мин чтения
В 2022 году вышла спецификация ES2022 (ECMAScript 2022), также известная как ES13. Ниже — основные изменения и новые возможности.

Методы Array.prototype.at()
Методы Object.hasOwn()
Логические операторы присваивания
Топ-уровневое await
Методы Error.cause
Методы RegExp Match Indices
Promise.any
Итог
Методы Array.prototype.at()
Метод at() предоставляет удобный способ доступа к элементам массива с использованием положительных и отрицательных индексов. Ранее, чтобы получить последний элемент массива, мы использовали конструкцию arr[arr.length - 1]. Теперь это можно сделать проще.

const array = ['JavaScript', 'Python', 'Ruby', 'Go'];

console.log(array.at(0)); // Выводит: "JavaScript"
console.log(array.at(-1)); // Выводит: "Go"
Copy
Этот метод упрощает доступ к последним элементам, особенно когда точный размер массива неизвестен, делая код более читаемым и элегантным.

Методы Object.hasOwn()
Метод Object.hasOwn() упрощает проверку наличия собственных свойств объекта — более прямая альтернатива Object.prototype.hasOwnProperty.call(), без конфликтов с собственными свойствами объекта.

const obj = { a: 1, b: 2 };

console.log(Object.hasOwn(obj, 'a')); // true
console.log(Object.hasOwn(obj, 'c')); // false
Copy
Логические операторы присваивания
ES2022 добавил три новых логических оператора присваивания: &&=, ||= и ??=. Они упрощают логические операции и делают код более читаемым.

&&= выполняет присваивание, если значение истинно.
||= выполняет присваивание, если значение ложно.
??= выполняет присваивание, если значение равно null или undefined.
let a = true;
let b = false;
let c = null;

a &&= false; // a = false
b ||= true;  // b = true
c ??= 3;     // c = 3
Copy
Топ-уровневое await
С ES2022 стало возможным использование await на верхнем уровне модулей, упрощая структуру кода и избавляя от необходимости оборачивать асинхронные вызовы в анонимные самовызывающиеся функции. Это значительно улучшает читаемость и поддержку кода, делая асинхронную логику более интуитивно понятной.

// example.mjs
const data = await fetch('https://api.example.com/data');
const jsonData = await data.json();

console.log(jsonData);
Copy
Методы Error.cause
Методы Error.cause позволяют передавать дополнительную информацию о причине ошибки. Это улучшает отладку и делает сообщения об ошибках более информативными.

try {
  throw new Error('Something went wrong', { cause: 'Invalid input' });
} catch (err) {
  console.error(err.message); // "Something went wrong"
  console.error(err.cause);   // "Invalid input"
}
Copy
Методы RegExp Match Indices
Добавлены методы для работы с индексами сопоставления в регулярных выражениях. Метод matchAll возвращает итератор с подробной информацией о каждом совпадении, включая индексы. Чтобы получить индексы совпадений, нужно использовать флаг d в регулярном выражении.

const regex = /t(e)(st(\d?))/gd;
const str = 'test1test2';

const matches = [...str.matchAll(regex)];

for (const match of matches) {
  console.log(`Full match: ${match[0]} at [${match.indices[0][0]}, ${match.indices[0][1]}]`);
  console.log(`First capturing group: ${match[1]} at [${match.indices[1][0]}, ${match.indices[1][1]}]`);
  console.log(`Second capturing group: ${match[2]} at [${match.indices[2][0]}, ${match.indices[2][1]}]`);
  console.log(`Third capturing group: ${match[3]} at [${match.indices[3][0]}, ${match.indices[3][1]}]`);
}

// Full match: test1 at [0, 5]
// First capturing group: e at [1, 2]
// Second capturing group: st1 at [2, 5]
// Third capturing group: 1 at [4, 5]

// Full match: test2 at [5, 10]
// First capturing group: e at [6, 7]
// Second capturing group: st2 at [7, 10]
// Third capturing group: 2 at [9, 10]
Copy
Как это работает:

Флаг d: Новый флаг d в регулярном выражении используется для отслеживания индексов совпадений.
Метод matchAll: Возвращает итератор, который содержит объект с информацией о каждом совпадении, включая индексы.
Promise.any
Метод Promise.any() ожидает исполнения одного из переданных промисов и возвращает его результат. Если ни один из промисов не выполнится успешно, возвращается ошибка AggregateError.

const promises = [
  Promise.reject('Ошибка'),
  Promise.resolve('Первый успешный ответ'),
  new Promise((resolve) => setTimeout(resolve, 100, 'Второй успешный ответ'))
];

Promise.any(promises).then((value) => console.log(value));
// Выводит: "Первый успешный ответ"
Copy
Итог
ES2022 добавил несколько точечных, но полезных вещей: удобный доступ к элементам по индексу через at(), прямую проверку свойств через Object.hasOwn(), top-level await, причины ошибок через Error.cause, индексы совпадений в регулярных выражениях и Promise.any. Каждое нововведение закрывает конкретную неудобность языка.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
17.02.2022
JavaScript ES2021 / ES12 новые возможности
6 мин чтения
← Старше
JAMstack: современный подход к веб-разработке
26.04.2023
Новее →
JavaScript ES2023 / ES14 новые возможности
27.05.2024
Copyright © 2026. Frontend Stuff

https://frontend-stuff.com/blog/es2021/

Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
JavaScript ES2021 / ES12 новые возможности
Что нового в ECMAScript 2021.

17.02.2022
·
6 мин чтения
Вот новые функции, добавленные в ES12.

Приватные методы
Метод String replaceAll()
WeakRef
Финализаторы
Promise.any() и AggregateError
Оператор логического присваивания
Подчеркивание как числовой разделитель
Итог
Приватные методы
Приватные методы могут быть доступны только внутри класса, в котором они определены. Имена приватных методов начинаются с символа #.

class Person {
  // Приватный метод
  #protected() {
    console.log("I am Private");
  }

  // Публичный метод
  print() {
    this.#protected();
  }
}

const personObj = new Person();
personObj.print(); // "I am Private";
personObj.protected(); // TypeError: personObj.protected is not a function
Copy
Поскольку protected() является приватным методом, personObj.protected возвращает значение undefined. Попытка использовать undefined в качестве функции вызывает ошибку TypeError.

Метод String replaceAll()
В прототип String добавлена новая функция. До этого добавления невозможно было заменить все экземпляры подстроки без использования регулярного выражения.

String.prototype.replaceAll() заменяет все вхождения строки другим строковым значением.

В настоящее время в строке JavaScript есть метод replace(). Его можно использовать для замены подстроки другой строкой.

// using Regex to replace
"1 2 1 2 1 2".replace("2", "0");
// → '1 0 1 2 1 2'
Copy
Если входной шаблон для замены является строкой, то метод replace() заменяет только первое вхождение. Поэтому в коде все вложения, кроме первого, не заменяются.

Ты сможешь сделать полную замену, только если предоставишь шаблон для замены в виде регулярного выражения.

// используя Regex для замены
"1 2 1 2 1 2".replace(new RegExp("2", "g"), "0");
// → '1 0 1 0 1 0'

// другой вариант
"1 2 1 2 1 2".replace(/2/g, "0");
// → '1 0 1 0 1 0'
Copy
Благодаря replaceAll это можно сделать так:

// String.prototype.replaceAll(searchValue, replaceValue)

"1 2 1 2 1 2".replaceAll("2", "0");
// → '1 0 1 0 1 0'
Copy
Что произойдет, если аргумент поиска - пустая строка? Он вернет замещенное значение между каждой единицей кода UCS-2 / UTF-16.

"x".replace("", "_");
// → '_x'

"xxx".replace(/(?:)/g, "_");
// → '_x_x_x_'

"xxx".replaceAll("", "_");
// → '_x_x_x_'
Copy
WeakRef
В JavaScript есть автоматический процесс сборки мусора. Он может собирать только недоступные объекты. Каждый раз, когда мы делаем присваивание объекту, то создаем сильную ссылку. Это защищает его от сборки мусора.

// x сильная ссылка на объект
const x = { foo: "bar" };
Copy
WeakRef означает слабые ссылки (Weak References). Основное использование слабых ссылок - реализация кешей или маппинг с большими объектами. В таких сценариях мы не хотим удерживать большое количество памяти в течение длительного времени, сохраняя этот редко используемый кеш или маппинг. Мы можем разрешить сборку мусора для памяти в ближайшее время, а позже, если она нам снова понадобится, можем сгенерировать новый кеш.

WeakRef служит совершенно другому варианту использования: для хранения ссылки на объект, который не защитит объект от сборки мусора. Поскольку сборка мусора не является детерминированной, нет реального понимания, когда конкретно объект будет очищен и произойдет ли это вообще. WeakRef использует данное преимущество, предоставляя тебе доступ к объекту, пока он не будет собран.

WeakRef может принимать объект только в качестве аргумента.

function Foo() {}

// сильная ссылка на экземпляр Foo
const x = new Foo();

// слабая ссылка на экземпляр Foo
const xWeak = new WeakRef(x);
Copy
Как вернуть значение? Используй метод deref(). И имей в виду, что метод вернет сильную ссылку на объект.

function Foo() {}

// сильная ссылка на экземпляр Foo
const x = new Foo();

// слабая ссылка на экземпляр Foo
const xWeak = new WeakRef(x);

// сильная ссылка на экземпляр Foo
const xFromWeak = xWeak.deref();
Copy
Почему это полезно? Это позволяет повысить производительность приложения в зависимости от устройства пользователя. Мы можем использовать WeakRefs для кеширования больших объектов. Это означает, что машины с большим объемом памяти могут увидеть увеличение производительности приложения. Те, у кого ограниченная память, по-прежнему будут работать, не съедая память пользователя.

JavaScript - это язык со сборкой мусора. Ты можешь узнать больше о сборщике мусора JavaScript здесь, на сайте MDN.

Финализаторы
Финализаторы - еще одна функция ES12 в области памяти. Эта функция сообщает нам, когда объект был собран сборщиком мусора. Это делается с помощью обратного вызова (callback) JavaScript.

Однако следует помнить о нескольких вещах:

не гарантируется, что обратный вызов будет выполнен;
целевой объект уже очищен и недоступен;
длительность выполнения обратного вызова, не является детерминированным. Это может быть как одна минута, так и один час.
// построение метода финализатора
const registry = new FinalizationRegistry((value) => {
  // код очистки должен быть здесь
  console.log(value);
});
Copy
registry является экземпляром FinalizationRegistry. Функция обратного вызова, переданная в FinalizationRegistry, срабатывает, когда объект собирается сборщиком мусора.

(function () {
  const obj = {};
  // подключаем переменную `obj` к финализатору
  registry.register(obj, "Deleted");
})();
Copy
На 3-й строке мы подключаем объект к registry. Когда сборщик мусора забирает obj, второй аргумент метода .register() передается функции обратного вызова. Итак, согласно логике нашего кода, когда obj забирается сборщиком мусора, строка "Deleted" передается функции обратного вызова и печатается в консоль.

Когда ты выполнишь приведенный выше код в консоли Google Chrome, примерно через 1 минуту он напечатает "Deleted". Еще один способ принудительно выполнить сборку мусора в Chrome - щелкнуть значок Collect Garbage. Ты можешь найти его во вкладке Performance.

Promise.any() и AggregateError
Есть еще одна полезная утилита - новый метод Promise.any. В качестве аргумента он принимает повторяющиеся промисы. Когда любое из них выполнено, он вызовет обратный вызов (callback) параметра Promise.any() или вернет ответ. Это зависит от того, используешь ли ты async/await или нет.

Когда все промисы терпят неудачу - метод выдаст ошибку AggregateError, которая объединяет все различные ошибки промисов. Почему бы не вернуть простой массив? В основном для совместимости. Это будет экземпляр ошибки, и ты получишь трассировку стека. Хорошо иметь эту информацию на случай, если она понадобится.

Давай посмотрим на несколько примеров.

Использование традиционного синтаксиса обратного вызова:

Promise.any([
  Promise.reject("Error 1"),
  Promise.reject("Error 2"),
  Promise.resolve("success"),
]).then((result) => {
  console.log("result:", result);
});
// result: success
Copy
Использование синтаксиса async/await:

(async () => {
  const result = await Promise.any([
    Promise.reject("Error 1"),
    Promise.reject("Error 2"),
    Promise.resolve("success"),
  ]);
  console.log(`result: ${result}`);
})();
// result: success
Copy
Теперь давай проверим пример сценария ошибки. В этом случае Promise.any() генерирует исключение AggregateError. Нам нужно поймать и справиться с этим.

try {
  (async function () {
    const result = await Promise.any([Promise.reject("Error 1")]);
    console.log(`result: ${result}`);
  })();
} catch (error) {
  console.log(error.errors);
}
Copy
В демонстрационных целях в Promise.any() передается только одно обещание. И этот промис завершается «с ошибкой» (reject). Приведенный выше код регистрирует следующую ошибку в консоли. И это четвертое дополнение к прототипу Promise.

На сегодняшний день в нашем распоряжении имеется следующее:

[ES2020] Promise.allSettled: этот метод возвращает промис, который выполняется, когда все промисы были выполнены или отклонены. Возвращенный объект описывает каждый отдельный результат промиса;
[ES2015] Promise.all: этот метод возвращает промис, который выполняется, только если все целевые промисы были выполнены;
[ES2015] Promise.race: этот метод возвращает промис, который будет выполнен, как только один из промисов будет отклонен или выполнен.
Оператор логического присваивания
Наконец, эти новые спецификации ES12 поставляются с долгожданными операторами. Новые операторы объединяют оператор логического присваивания с логическими операциями &&, || и ??.

Оператор логического присваивания &&=
Давай сравним его эквивалент в ES11 с новой спецификацией.

До:

x && (x = y);
Copy
Сейчас:

x &&= y;
Copy
Пример:

let x = 5;
const y = 10;
x &&= y;

console.log(x);
// 10
Copy
Или по-другому:

if (x) {
  x = y;
}
Copy
Поскольку x - истинное значение, ему присваивается значение y, то есть 10.

Как и в случае с &&, мы можем поступить и с ||, а также ??.

Оператор логического присваивания ||=
Сравниваем его эквивалент в ES11 с новой спецификацией.

До:

x || (x = y);
Copy
Сейчас:

x ||= y;
Copy
Пример:

let x = 5;
const y = 10;
x ||= y;

console.log(x);
// result is 5
Copy
Это означает, что операция присваивания происходит, только если x является ложным значением. В нашем коде x содержит 5, которое является истинным значением, и, следовательно, присваивания не происходит. Вот почему наш код печатает 5 в консоли.

Оператор логического присваивания ??=
?? - это нулевой оператор объединения в JavaScript. Давай вспомним, что он делает, поскольку встречается реже.

Оператор объединения с нулевым значением (??) - это логический оператор, который возвращает его правый операнд, когда его левый равен null или undefined, в противном случае возвращает его левый операнд. - MDN

Давай сравним его эквивалент в ES11 с новой спецификацией.

До:

x ?? (x = y);
Copy
Сейчас:

x ??= y;
Copy
Пример:

let x;
const y = 10;
x ??= y;

console.log(x);
// result is 10
Copy
Строка 3 в приведенном выше коде эквивалентна:

x = x ?? (x = y);
Copy
Если значение x равно null или undefined, правая часть от ?? оценивается и присваивается y.

Здесь значение x равно undefined. Таким образом, выражение в правой части вычисляется и устанавливает x равным 10.

Подчеркивание как числовой разделитель
Это дополнение может быть менее необычным, но оно помогает улучшить читаемость твоего кода. В настоящее время при хранении длинного числа в переменной var/let/const оно может быть нечитабельным.

А сейчас давай посмотрим на вот этот пример. Создадим число 1 миллион. Становится очевидным, что длинные числа трудно читать.

const oneMillion = 1000000;
Copy
С новым синтаксисом ES12 ты можешь разделить цифры с помощью символа _.

const oneMillion = 1_000_000;
console.log(oneMillion); // 1000000
Copy
Разделитель подчеркивания _ также работает с числами BigInt.

const trillion = 1000_000_000_000n;
console.log(trillion.toString()); // "1000000000000"
Copy
Его также можно использовать для двоичных и шестнадцатеричных литералов:

const max8bits = 0b1111_1111;
console.log(max8bits); // 255

const message = 0xa0_b0_c0;
console.log(message); // 10531008
Copy
Это определенно то, что лично я буду использовать довольно часто.

Итог
ES2021 добавил несколько небольших, но полезных возможностей: логические операторы присваивания, replaceAll и числовые разделители. Всё это уже доступно в современных браузерах.

WeakRefs и Finalizers — более низкоуровневые инструменты. Перед использованием стоит разобраться в их семантике: они закрывают конкретные сценарии работы с памятью и не предназначены для повседневного применения.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
23.05.2024
JavaScript ES2022 / ES13 новые возможности
3 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
← Старше
Как удалить все каталоги node_modules
28.09.2021
Новее →
JAMstack: современный подход к веб-разработке
26.04.2023
Copyright © 2026. Frontend Stuff


https://www.youtube.com/watch?v=cPIs8q8lGAA
https://selvaganesh93.medium.com/javascript-whats-new-in-ecmascript-2019-es2019-es10-35210c6e7f4b

Open in app
Sign up

Sign in

Search
Write


JavaScript: What’s new in ECMAScript 2019 (ES2019)/ES10?
Selvaganesh
Selvaganesh

Follow
3 min read
·
Feb 8, 2019
5K


5



Press enter or click to view image in full size

Chrome Version 72 rolled out new exciting ES10 features for developer to use in browser.

1. Array.Flat()
The flat() method creates a new array with all sub-array elements concatenated into it recursively up to the specified depth.


2.Array.flatMap()
The flatMap() method first maps each element using a mapping function, then flattens the result into a new array. It is identical to a map followed by a flat of depth 1, but flatMap is often quite useful, as merging both into one method is slightly more efficient.


3.Object.fromEntries()
The Object.fromEntries() method transforms a list of key-value pairs into an object.

Note: Object.fromEntries only accept iterable (i.e) Object.fromEntries(iterable)

It will accept only Map or Array

Example 1


Example 2


4.String.trimStart() & String.trimEnd()
The trimStart() method removes whitespace from the beginning of a string.

The trimEnd() method removes whitespace from the end of a string.

Get Selvaganesh’s stories in your inbox
Join Medium for free to get updates from this writer.

Enter your email
Subscribe

Remember me for faster sign in

You can think why another new method already there are two method trimRight()and trimLeft() but it will be alias of above new methods


5.Optional Catch Binding
Allow developers to use try/catch without creating an unused binding.
You are free to go ahead make use of catch block without a param


Earlier it is mandatory to use param in catch block


6.Function.toString()
The toString() method returns a string representing the source code of the function.Earlier white spaces,new lines and comments will be removed when you do now they are retained with original source code


7.Symbol.description
The read-only description property is a string returning the optional description of Symbol objects.


8.Well Formed JSON.Stringify()
To prevent JSON.stringify from returning ill-formed Unicode strings.


9.Array.Sort Stability
Users with same rating retain their sorting order


Previously, V8 used an unstable QuickSort for arrays with more than 10 elements. As of V8 v7.0 / Chrome 70, V8 uses the stable TimSort algorithm. 🎉

The only major engine JavaScript engine that still has an unstable Array#sort implementation is Chakra, which uses QuickSort for arrays with more than 512 elements (and stable insertion sort for anything else).

For more info read this article

10.JSON ⊂ ECMAScript (JSON Superset)
Extend ECMA-262 syntax into a superset of JSON.

JSON syntax is defined by ECMA-404 and permanently fixed by RFC 7159, but the DoubleStringCharacter and SingleStringCharacter productions of ECMA-262 can be extended to allow unescaped U+2028 LINE SEPARATOR and U+2029 PARAGRAPH SEPARATOR characters.

For more info read this proposal


If you like this article share and clap 👏 👏. Thanks!

JavaScript
Es2019
Es10
New Features
Chrome
5K


5


Selvaganesh
Written by Selvaganesh
549 followers
·
203 following
I try to learn and help others by sharing what I find | https://www.youtube.com/@codewithselva | https://github.com/ganny26


Follow
Responses (5)

Write a response

What are your thoughts?

Cancel
Respond
See all responses
https://frontend-stuff.com/blog/es2018/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
ES2018: object rest/spread, finally и новые возможности RegExp
Что принес ES2018: object rest/spread, асинхронные итераторы, Promise.prototype.finally и важные улучшения регулярных выражений.

25.04.2019
·
3 мин чтения
ES2018 не выглядит таким громким релизом, как ES6, но именно в таких версиях язык становится удобнее в повседневной работе. Здесь появились улучшения, которые быстро превращаются в привычку: object rest/spread, Promise.prototype.finally(), асинхронные итераторы и более сильные регулярные выражения.

Важно и то, что большая часть этих возможностей не декоративная. Они сокращают служебный код, упрощают работу с данными и убирают старые обходные пути, которые раньше приходилось писать вручную.

Ниже разберем изменения ES2018 на примерах, которые реально встречаются в приложениях и инструментах.

Object rest и spread
Асинхронная итерация
Promise.prototype.finally()
Новые возможности регулярных выражений
Итог
Object rest и spread
В ES6 мы уже получили rest и spread для массивов. ES2018 перенес ту же идею на объекты.

Rest для объектов
rest помогает забрать часть полей отдельно, а остальные собрать в новый объект:

const user = {
  id: 1,
  name: "Alex",
  role: "author",
  city: "Madrid",
};

const { id, ...publicProfile } = user;

console.log(id); // 1
console.log(publicProfile);
// { name: "Alex", role: "author", city: "Madrid" }
Copy
Это удобно, когда нужно убрать служебные поля перед отправкой данных в UI или API.

Spread для объектов
spread разворачивает свойства объекта в новый объект. Это особенно полезно для иммутабельных обновлений:

const user = {
  name: "Alex",
  city: "Madrid",
};

const updatedUser = {
  ...user,
  city: "Barcelona",
  role: "author",
};

console.log(updatedUser);
// { name: "Alex", city: "Barcelona", role: "author" }
Copy
Если одинаковый ключ встречается несколько раз, побеждает последнее значение.

Асинхронная итерация
for await...of позволяет последовательно читать асинхронный источник данных. Чаще всего его используют с async iterable, но он также умеет ждать значения из массива промисов.

const requests = [
  Promise.resolve("first"),
  Promise.resolve("second"),
  Promise.resolve("third"),
];

async function printInOrder() {
  for await (const value of requests) {
    console.log(value);
  }
}

printInOrder();
// first
// second
// third
Copy
Важно понимать ограничение: for await...of ждет значение перед переходом к следующей итерации. Если нужна именно параллельная загрузка, сначала запускай все операции, а потом собирай результат через Promise.all.

Promise.prototype.finally()
До ES2018 одинаковый код очистки приходилось дублировать и в then, и в catch. finally() убирает это повторение:

fetch("https://api.github.com/users/oleksiimyzgin")
  .then((response) => response.json())
  .then((data) => {
    console.log(data.login);
  })
  .catch((error) => {
    console.error(error);
  })
  .finally(() => {
    console.log("request finished");
  });
Copy
finally() не получает результат промиса. Его задача не обработать данные, а выполнить код, который должен сработать в любом случае: скрыть loader, освободить ресурс, завершить таймер или сбросить флаг состояния.

Новые возможности регулярных выражений
ES2018 заметно усилил RegExp. Самые полезные изменения: lookbehind, именованные группы, Unicode property escapes и флаг s.

Lookbehind
lookahead проверяет, что нужная строка идет после текущего совпадения. lookbehind делает то же самое, но в обратную сторону.

const lookAhead = /Java(?=Script)/;
const lookBehind = /(?<=Java)Script/;

console.log(lookAhead.test("JavaScript")); // true
console.log(lookAhead.test("Java Script")); // false

console.log(lookBehind.test("JavaScript")); // true
console.log(lookBehind.test("TypeScript")); // false
Copy
Отрицательный lookbehind тоже работает:

const notAfterJava = /(?<!Java)Script/;

console.log(notAfterJava.test("JavaScript")); // false
console.log(notAfterJava.test("TypeScript")); // true
Copy
Именованные группы
С именованными группами результат регулярного выражения становится заметно понятнее:

const datePattern = /(?<year>\d{4})-(?<month>\d{2})-(?<day>\d{2})/;
const match = datePattern.exec("2019-04-10");

console.log(match.groups.year); // "2019"
console.log(match.groups.month); // "04"
console.log(match.groups.day); // "10"
Copy
Это особенно полезно в сложных шаблонах, где доступ к match[1] и match[2] быстро превращается в источник ошибок.

Unicode property escapes
\\p{...} и \\P{...} позволяют описывать классы символов через свойства Unicode:

const asciiOnly = /^\p{ASCII}+$/u;
const greekOnly = /^\p{Script=Greek}+$/u;
const emojiOnly = /^\p{Emoji}+$/u;

console.log(asciiOnly.test("ABC123")); // true
console.log(asciiOnly.test("ABC🙃")); // false

console.log(greekOnly.test("ελληνικά")); // true
console.log(greekOnly.test("hello")); // false

console.log(emojiOnly.test("🙃🙃")); // true
console.log(emojiOnly.test("A")); // false
Copy
Флаг s
Флаг s включает режим dotAll: точка начинает совпадать и с символом новой строки.

const withoutDotAll = /hi.welcome/;
const withDotAll = /hi.welcome/s;

console.log(withoutDotAll.test("hi\nwelcome")); // false
console.log(withDotAll.test("hi\nwelcome")); // true
Copy
Итог
ES2018 не меняет язык радикально, но сильно улучшает ежедневную работу. object rest/spread делают обновление данных чище, finally() убирает дублирование, for await...of упрощает работу с асинхронными потоками, а RegExp впервые становятся заметно удобнее для серьезных задач.

Если выбираешь, что внедрять в старом коде в первую очередь, начинай с object rest/spread и finally() — изменения минимальные, а читаемость растет сразу.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
23.05.2024
JavaScript ES2022 / ES13 новые возможности
3 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
← Старше
Руководство по ES2017 (ES8)
18.04.2019
Новее →
Что такое JavaScript Promises
02.05.2019
Copyright © 2026. Frontend Stuff



https://frontend-stuff.com/blog/es2017/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
Руководство по ES2017 (ES8)
Узнай всё о ECMAScript и функциях, добавленных в ES2017 (ES8).

18.04.2019
·
4 мин чтения
ECMAScript - это стандарт, на котором основан JavaScript, и его часто называют ES.

ECMAScript 2017 - 8 издание стандарта ECMA-262 (также известный как ES2017 или ES8), был завершен в июне 2017 года.

По сравнению с ES6, ES8 является маленьким выпуском для JavaScript, но всё же содержит очень полезные функции:

Заполнения строки

Object.values()

Object.entries()

getOwnPropertyDescriptors()

Когда это полезно
«Висячие» запятые

async/await или асинхронные функции

Несколько асинхронных функций
Разделяемая память и атомарные операции

Заполнения строки

Object.values()

Object.entries()

getOwnPropertyDescriptors()

Когда это полезно
«Висячие» запятые

async/await или асинхронные функции

Несколько асинхронных функций
Разделяемая память и атомарные операции

Заполнения строки
Целью заполнения строки является добавление символов в строку, чтобы она достигла определенной длины.

ES2017 представляет два метода для строк padStart() и padEnd() - которые позволяют добавлять либо пустую строку, либо любую другую строку к началу или, концу исходной строки.

"7".padStart(3, "0"); // "007"
"7".padEnd(3, "."); // "7.."
Copy
Пример использования:

// padStart()

"test".padStart(4); // 'test'
"test".padStart(5); // ' test'
"test".padStart(8); // '    test'
"test".padStart(8, "abcd"); // 'abcdtest'
Copy
// padEnd()

"test".padEnd(4); // 'test'
"test".padEnd(5); // 'test '
"test".padEnd(8); // 'test    '
"test".padEnd(8, "abcd"); // 'testabcd'
Copy
Object.values()
Object.values() - это новая функция, которая похожа на Object.keys(), но возвращает массив, содержащий все значения свойств объекта, исключая любые значения в цепочке прототипов.

Например:

const person = { name: "Alex", age: 28 };
Object.values(person); // ["Alex", 28]
Copy
Object.values() также работает с массивами:

const people = ["Alex", "Julia"];
Object.values(people); // ["Alex", "Julia"]
Copy
Object.entries()
Object.entries() относится к Object.keys, но возвращает массив, содержащий все собственные свойства объекта, в виде массива пар [key, value].

Например:

const person = { name: "Alex", age: 28 };
Object.entries(person); // [['name', '"Alex"'], ['age', 28]]
Copy
Object.entries() также работает с массивами:

const people = ["Alex", "Julia"];
Object.entries(people); // [['0', 'Alex'], ['1', 'Julia']]
Copy
getOwnPropertyDescriptors()
Этот метод возвращает все собственные (не унаследованные) дескрипторы свойств объекта.

Любой объект в JavaScript имеет набор свойств, и каждое из этих свойств имеет дескриптор.

Дескриптор - это набор атрибутов свойства, состоящий из подмножества следующих:

value: значение свойства;
writable: true если свойство может быть изменено;
get: функция получения (геттер) для свойства, вызываемая при чтении свойства;
set: функция установки (сеттер) для свойства, вызываемая, когда для свойства установлено значение;
configurable: если false, свойство не может быть удалено, и любой атрибут не может быть изменен, кроме его значения;
enumerable: true, если свойство перечислимо;
Object.getOwnPropertyDescriptors(obj) принимает объект и возвращает все собственные дескрипторы свойств данного объекта.

Когда это полезно
ES6 предоставил нам Object.assign(), который копирует все перечисленные собственные свойства из одного или нескольких объектов и возвращает новый объект.

Однако есть проблема - он неправильно копирует свойства с атрибутами не по умолчанию.

Если объект, например, имеет только сеттер, то он неправильно копируется в новый объект с помощью Object.assign().

Например:

const person1 = {
  set name(newName) {
    console.log(newName);
  }
};
Copy
Это не сработает:

const person2 = {};
Object.assign(person2, person1);
Copy
А это будет работать:

const person3 = {};
Object.defineProperties(person3, Object.getOwnPropertyDescriptors(person1));
Copy
Ты можешь увидеть это с помощью простого консольного теста:

person1.name = "x";
("x");

person2.name = "x";

person3.name = "x";
("x");
Copy
у person2 нет сеттера, так как он не был скопирован.

То же самое ограничение касается поверхностного клонирования объектов с помощью Object.create().

«Висячие» запятые
Эта функция позволяет использовать запятые в объявлениях и в вызовах функций после последнего параметра функции:

const doSomething = (var1, var2,) => {
  return `${var1}-${var2}`;
};

doSomething("test2", "test2",);
Copy
async/await или асинхронные функции
Более подробно об async/await здесь.

ES2017 представил концепцию асинхронных функций (async functions), и это самое важное изменение, представленное в этом издании ECMAScript.

async/await - абстракция более высокого уровня по сравнению с промисами.

Когда Promises были введены в ES6, они предназначались для решения проблемы с асинхронным кодом, и они это сделали. Но за 2 года, в течение которых ES6 и ES2017 были разделены стало ясно, что промисы не могут быть окончательным решением. Промисы были введены для решения известной проблемы “ад обратных вызовов”, но ввели сложность сами по себе и сложность синтаксиса.

Асинхронная функция представляет собой функцию, которая работает асинхронно с помощью цикла событий, используя неявное обещание вернуть результат. Но синтаксис и структура кода с использованием асинхронных функций больше похожи на использование стандартных синхронных функций.

Ключевое слово async указывает компилятору JavaScript обрабатывать функцию по-другому. Компилятор делает паузу, когда достигает ключевого слова await в этой функции. Он предполагает, что выражение после await вернет промис и ждет, пока промис не будет выполнен успешно или отклонен, прежде чем двигаться дальше. Важно, ключевое слово await допустимо только внутри async функций.

Код, использующий асинхронные функции, может быть записан как:

function doSomethingAsync() {
  return new Promise(resolve => {
    setTimeout(() => resolve("I did something"), 3000);
  });
}

async function doSomething() {
  console.log(await doSomethingAsync());
}

console.log("Before");
doSomething();
console.log("After");
Copy
Приведенный выше код выведет в консоль браузера следующее:

Before
After
I did something // через 3 секунды
Copy
Несколько асинхронных функций
Асинхронные функции могут быть очень легко соединены в цепочку, а их синтаксис гораздо более читабелен, чем у промисов:

async function go() {
  const p1 = await fetch("https://api.github.com/users/oleksiimyzgin");
  const p2 = await fetch("https://api.github.com/users/leoyats");
}

go();
Copy
В примере выше сначала мы ждем пока выполнится p1, а потом уже p2.

Разделяемая память и атомарные операции
Разделяемая память (shared memory) и атомарные операции (atomics) - это огромный, довольно продвинутый функционал, который является основным улучшением движков JS.

Основная идея состоит в том, чтобы привнести в JavaScript какую-то многопоточность, для того чтоб разработчики JS в будущем могли писать высокопроизводительные параллельные программы и управлять памятью самостоятельно, а не позволять это делать движку JS.

Для этого мы задействуем новый тип глобального объекта SharedArrayBuffer, который по существу хранит данные в общем пространстве памяти. Таким образом, эти данные могут быть разделены между основным потоком JS и потоками web-worker.

WebWorkers предлагают протокол обмена сообщениями через события. Начиная с ES2017, мы можем создавать массив разделяемой памяти между web-workers и их создателями, используя SharedArrayBuffer.

Поскольку неизвестно, сколько времени занимает запись в разделяемую часть памяти для распространения, мы используем Atomics - способ удостоверится, что при чтении значения, любой вид операции записи завершен.

Любая более подробная информация может быть найдена в предложении спецификации.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
23.05.2024
JavaScript ES2022 / ES13 новые возможности
3 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
← Старше
Руководство по ES2016 (ES7)
11.04.2019
Новее →
ES2018: object rest/spread, finally и новые возможности RegExp
25.04.2019
Copyright © 2026. Frontend Stuff



https://frontend-stuff.com/blog/es2016/
Frontend Stuff
Статьи
Темы
Контакты
Поиск

← Все статьи
JS
JavaScript
Руководство по ES2016 (ES7)
Узнай всё о ECMAScript и функциях, добавленных в ES2016 (ES7).

11.04.2019
·
1 мин чтения
ECMAScript - это стандарт, на котором основан JavaScript, и его часто называют ES.

ES2016, официально известный как ECMAScript 2016, был завершен в июне 2016 года.

По сравнению с ES2015, ES2016 - это крошечный релиз для JavaScript, содержащий всего две функции:

Array.prototype.includes()
Оператор возведения в степень
Array.prototype.includes()
Функция includes() вводит более читаемый синтаксис для проверки, содержит ли массив элемент, возвращая в зависимости от этого true или false.

Ранее, чтобы проверить, содержит ли массив элемент, ты должен был использовать indexOf, который проверял индексы в массиве и возвращал -1, если элемента там нет.

Поскольку -1 оценивается как истинное значение, мы не можем сделать, например так:

if (![1, 2].indexOf(3)) {
  console.log("Not found");
}
Copy
С помощью функции includes(), представленной в ES2016, мы можем сделать так:

if (![1, 2].includes(3)) {
  console.log("Not found");
}
Copy
Оператор возведения в степень
Оператор возведения в степень ** является эквивалентом Math.pow(), но добавлен в язык, вместо того чтобы быть функцией библиотеки.

Math.pow(4, 2) === 4 ** 2;
Copy
Эта функция является хорошим дополнением для математических приложений JS.

Что почитать дальше
24.03.2026
Temporal в JavaScript: как наконец перестать ошибаться в работе с датами
3 мин чтения
06.06.2024
Циклы в JavaScript: как выбрать нужный
6 мин чтения
27.05.2024
JavaScript ES2023 / ES14 новые возможности
2 мин чтения
23.05.2024
JavaScript ES2022 / ES13 новые возможности
3 мин чтения
26.04.2023
JAMstack: современный подход к веб-разработке
2 мин чтения
← Старше
Руководство по ES6
04.04.2019
Новее →
Руководство по ES2017 (ES8)
18.04.2019
Copyright © 2026. Frontend Stuff

https://survey.stackoverflow.co/2025/technology
JavaScript: основы языка и работа с DOM (презентация)
Требуемые условия завершения
1. Переменные, типы данных, операторы
Теория:

Переменные:

Объявление: let, const, var (разница между ними).

Имена переменных: правила (нельзя начинать с цифры, регистрозависимость).

Типы данных:

Примитивы: number, string, boolean, null, undefined, symbol, bigint.

Объекты: object, array, function.

Проверка типа: typeof.

Операторы:

Арифметические: +, -, *, /, %, **.

Сравнения: ==, ===, !=, !==, >, <, >=, <=.

Логические: &&, ||, !.

Присваивания: =, +=, -=.

2. Функции, циклы, условия
Теория:

Функции:

Объявление: function sum(a, b) { return a + b; }.

Стрелочные функции: const sum = (a, b) => a + b;.

Условия:

if...else, switch.

Тернарный оператор: condition ? true : false.

Циклы:

for: for (let i = 0; i < 5; i++) { ... }.

while: while (condition) { ... }.

do...while: do { ... } while (condition).

3. Работа с DOM (Document Object Model)
Теория:

Выбор элементов:

document.getElementById(), document.querySelector(), document.querySelectorAll().

Изменение содержимого:

element.textContent, element.innerHTML.

element.setAttribute(), element.style.

События:

click, mouseover, keydown.

Добавление обработчиков: element.addEventListener().

Инструменты
Браузерные инструменты:

Консоль разработчика: Для отладки кода (F12).

JSFiddle/CodePen: Онлайн-песочницы для экспериментов.

Редакторы кода:

VS Code с плагинами:

Live Server: Автоперезагрузка страницы.

ESLint: Проверка стиля кода.

Фреймворки для тестирования:

Jest: Для написания юнит-тестов (например, проверить функцию isEven).

Нажмите на ссылку JavaScript: основы языка и работа с DOM (презентация), чтобы открыть ресурс.

